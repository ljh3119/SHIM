# Copy this file to `.env` for operation and replace the value below.
# Do not commit or share the real `.env` file.
#
# PowerShell random value example:
# [Convert]::ToBase64String((1..48 | ForEach-Object { Get-Random -Maximum 256 }))

SHIM_SECRET_KEY=replace_with_long_random_secret_key_before_operation

# 타임존 설정 (기본값: Asia/Seoul)
# 다른 국가나 타임존을 지정하려면 해당 시간대 명칭을 입력하십시오. (예: UTC, America/New_York)
TZ=Asia/Seoul
