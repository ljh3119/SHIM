# 향후 개선계획 (Product Roadmap)

이 문서는 SHIM 프로젝트의 중장기 발전 방향과 차기 버전의 백로그를 관리합니다. 

---

## 🎯 1. 개발 및 운영 원칙
- **Minimalism**: 폐쇄망 환경의 특수성을 고려하여 외부 의존성을 최소화합니다.
- **Consistency**: 기존의 시간 정책(30/60/120분 단위)과 Dense UI 디자인 시스템을 유지합니다.
- **Verification**: 모든 기능 추가 시 `tools/scripts` 내 검증 시나리오를 동기화합니다.

---

## 🚀 2. 주요 개선 항목 (Backlog)

### [P1] 우선순위 핵심 과제 (완료)
| ID | 과제명 | 목표 | 완료 기준 | 상태 |
|:--- |:--- |:--- |:--- |:--- |
| **P1-1** | **감사 로그 조회 UI 보강** | 관리자 마스터 화면의 로그 가독성 및 필터링 강화 | 수행자, 액션, 기간별 검색 지원 | 완료 (v1.4.0) |
| **P1-2** | **사원 관리 UX 정리** | 사원 목록 테이블의 작업 버튼 최적화 (가로 공간 확보) | 드롭다운 또는 아이콘 버튼 도입 | 완료 (v1.4.0) |
| **P1-3** | **타임라인 페이징 도입** | 대량 데이터(연 1만 건+) 조회 시 브라우저 부하 방지 | 서버 측 LIMIT/OFFSET 적용 | 완료 (v1.4.0) |

### [P2] 편의 및 확장 과제
- **근태 항목 확장**: 연차 외 '출장(연차 미소모)', '공가(유급 휴가지만 개인 연차 미소모)' 등 신규 유형 지원
    - 데이터베이스 `leave_type` 필드 확장 및 연차 차감 로직 예외 처리 구현
    - 팀 캘린더 내 유형별 색상 테마(예: 출장-초록, 공가-보라) 및 한국어 라벨 적용
- **다수일 일괄 신청**: 동일 시간대(예: 09:00~18:00)에 대해 여러 날짜를 한 번에 선택하여 신청하는 기능
- **통계 리포트**: 전사/팀별 연차 사용 현황 엑셀 다운로드 기능 (관리자용)
- **알림 강화**: 외부망 차단 환경 내에서 브라우저 Web Push 또는 자체 대시보드 알림 위젯
- **접근성 및 UI**:
    - Dense UI 내 고대비 모드 및 키보드 내비게이션 보강
    - 모바일 환경에서의 연차 신청 UX 최적화 (날짜 선택기 개선)
    - 대시보드 내 인터랙티브 캘린더 (클릭/드래그 신청 지원)

---

## 🏗️ 3. 기술 부채 및 아키텍처 과제
- **데이터베이스 고도화**:
    - Alembic 도입을 통한 정식 데이터베이스 스키마 버전 관리
    - `AuditLogs` 테이블의 `old_data`, `new_data`를 JSON 포맷으로 전환하여 상세 쿼리 지원
- **성능 최적화**:
    - `SystemSettings` 등 빈번하게 조회되는 설정값의 인메모리 캐싱 도입
    - 대규모 인원(100인+) 조회 시 관리자 캘린더/타임라인 렌더링 성능 개선 (서버 측 데이터 가공 최적화)
- **보안 및 인증**:
    - 세션 만료 정책 강화 및 중복 로그인 방지 옵션
    - FastAPI Dependency를 활용한 보다 엄격한 RBAC(Role-Based Access Control) 구조 공통화
- **테스트 커버리지**: 비즈니스 로직(연차 계산)에 대한 단위 테스트 강화 및 CI 자동화 검증
- **포터블 최적화**: PyInstaller 빌드 속도 개선 및 실행 바이너리 용량 최적화


