# 향후 개선계획 (Product Roadmap)

이 문서는 SHIM 프로젝트의 중장기 발전 방향과 차기 버전의 백로그를 관리합니다. 300명 규모 및 5년 운영 수준의 소규모 폐쇄망 구동 환경에 필요한 최적의 가성비와 슬림한 포터블 배포 철학(Zero-Configuration)에 맞춰 계획을 현실적으로 조정했습니다.

---

## 🎯 1. 개발 및 운영 원칙
- **Minimalism (초경량성)**: 외부 환경이나 추가 의존성 없이 장기 구동이 가능하도록 외부 종속성을 철저히 제한합니다.
- **Lightweight Portable (슬림한 배포)**: 포터블 바이너리(`SHIM_Portable.exe`)의 기동 속도와 런타임 성능을 최적화하기 위해, 구동에 불필요한 서드파티 라이브러리 및 임포트 의존성을 전면 배제하여 순수하게 유지합니다.
- **Verification**: 모든 기능 추가 시 `tools/scripts` 내 검증 시나리오를 동기화하여 안정성을 보장합니다.
- **Consistency**: 기존의 시간 정책(30/60/120분 단위)과 Dense UI 디자인 시스템의 고유 정체성을 준수합니다.

---

## 🚀 2. 주요 개선 과제 (Backlog)

실제 사용 환경(소규모 사용자, 단독 기기 구동)의 실무적 요구와 극단적인 포터블 편의성을 보장하기 위해 선별된 차기 개선 과제입니다.

### 1) [P2] 감사 로그 수행자 스냅샷 컬럼 추가 (소급 마이그레이션 생략)
- **목적**: 사원 강제 삭제(Hard Delete) 후에도 과거 감사 로그 이력의 직관성 보존.
- **내용**:
  - `AuditLogs` 테이블에 `actor_name` 및 `actor_department` 컬럼을 신설하여 신규 기록 시점의 정보를 직접 적재합니다.
  - 아직 정식 배포 이전으로 과거 유실 데이터에 대한 무겁고 위험한 소급 마이그레이션(DML) 작업은 전면 생략하여 스키마 개선만 가볍게 수행합니다.

### 2) [P2] PII 개인정보 컬럼 선택적 암호화 및 복구 이식성 보장
- **목적**: 데이터베이스 물리 파일(`.db`) 탈취 시 민감 사원 정보 및 연차 사유 유출 방지.
- **포터블 고려사항 및 조율**:
  - > [!WARNING]
    > 암호화를 무조건 적용할 경우, `.db` 파일만 복사해서 다른 기기에 이식(복구)했을 때 복호화 키가 맞지 않으면 시스템이 정상 작동하지 않아 제로-컨피그 포터블 복구 기능이 훼손됩니다.
  - **선택적 암호화 설계**: 암호화 키(`SHIM_SECRET_KEY`)가 환경변수나 로컬 설정으로 주어지지 않거나 기본값인 경우, 평문으로 저장하도록 자동 폴백(Fallback)시킵니다. 보안 수준 강화가 필요한 구축 환경에서만 키를 별도로 백업할 책임(키 수동 이전)을 안내하고 암호화를 활성화하도록 유연하게 설계합니다.
  - C 컴파일 의존성을 없애기 위해 기존 패키지에 있는 `cryptography` 라이브러리의 `Fernet`을 활용해 SQLAlchemy `TypeDecorator`로 가볍게 구현합니다.
  - **향후 가이드 및 릴리즈 문서화 필수 반영 사항**:
    - 본 기능의 개발 완료 시점에, `docs/1-2_백업_복구_유지보수_가이드.md` 문서의 복구 절차에 "DB 암호화 활성화 상태에서 다른 기기로 이동/복구 시 `var/data/shim_internal.db` 파일뿐만 아니라, **동일 폴더의 `secret.key` 파일도 함께 세트로 복사**해야 함"을 명시적으로 추가 기재해야 합니다.


### 3) [P2] 초경량 백그라운드 핫 백업 및 부팅 자가 복구 (Fail-safe)
- **목적**: 돌발적인 전원 차단 또는 강제 종료로 인한 SQLite 파일 손상(Corruption) 방지 및 무중단 자가 복구.
- **내용**:
  - **비동기 핫 백업**: 매 트랜잭션마다 백업을 돌리는 무거운 상시 리플리케이션 대신, FastAPI Lifespan의 `asyncio` 태스크 루프를 이용한 **'1일 1회 정기 백업'**과 연차 승인 등 중요 데이터 변경 이벤트 시점에 백그라운드로 가볍게 실행되는 **'이벤트 트리거 백업'**을 절충하여 성능 오버헤드를 제로화합니다.
  - **백업 파일 용량 제한 (순환 보관)**: 백업본이 무한정 디스크를 차지하는 것을 방지하기 위해 **최대 5개의 백업 파일만 유지하는 순환 정책(Rotation)**을 적용합니다. 새 백업 파일 생성 시 보관 개수를 초과하면 가장 수정 시간이 오래된 백업 파일을 자동으로 삭제(`os.remove()`)하도록 클린업 로직을 탑재하여 전체 백업 용량이 30~50MB 수준을 절대 넘지 않게 통제합니다.
  - SQLite 네이티브 API인 `sqlite3.backup()`을 활용하여 무중단/무잠금 상태로 백업본(`.db.bak`)을 안전하게 생성합니다.
  - **자가 복구**: 서버 시작 시 `PRAGMA quick_check;`를 실행하여 DB 물리 손상 여부를 검증하고, 오염이 감지되면 기존 파손본을 격리 보관한 뒤 가장 최근의 정상 백업본(`.db.bak`)으로 자동 복사/대체 기동하는 복원 로직을 탑재합니다.

---

## ⏳ 3. 장기 보류 및 재검토 과제 (P3)

### [P1] 정식 출시 기준 정비 및 마이그레이션 베이스라인화 (보류)
- **정식 출시 시점의 마이그레이션 스쿼시(Squash) 및 베이스라인화**:
  - 정식 배포 릴리즈 확정 시점(버전 미정) 직전까지 완전히 보류합니다.
  - 기존 테스트/개발 단계에서 적용되었던 점진적 마이그레이션 스텝들(`v1.4.0`, `v1.5.0`, `v1.5.6` 등)을 SQLAlchemy 기본 스키마 정의(`models.Base.metadata.create_all`)로 전면 통합합니다.
  - 정식 버전 이후 실운영 중에 발생하는 스키마 변경 사항만 신규 마이그레이션으로 정의하여 데이터베이스 관리 복잡도를 예방합니다.

### [P3] 기획 검토 및 철학적 조율 필요 과제
- **사원 비활성화 시 미래 연차 처리**:
  - 사원 비활성화 시 미래 연차의 자동 반려가 실무 예외 프로세스(예: 복직 등)와 충돌할 소지가 있으므로 구체적인 기획 검토 완료 후 진행 여부를 결정합니다.
- **비밀번호 강도 최소화 검증 기준 보강**:
  - 현재 비밀번호 자가 변경 시 최소 4자 이상이라는 매우 단순한 정책을 가지고 있으나, 보안 요구 수준에 맞춘 비밀번호 복잡도 규칙이나 기준을 강화할 수 있도록 기획 설계를 보완하는 개선안을 보류 과제로 등록합니다.


### 🌐 [P3] 외부망 연동 및 특수 환경 요구 과제 (최후순위 보류)
*SHIM의 핵심 정체성인 '오프라인 폐쇄망 최적화 및 제로 컨피그' 철학과 상충될 여지가 있는 기능들로, 외부 인터넷망 환경이나 별도 인프라 연동이 수반되는 특수 구축 사례에 한해서만 가장 늦게 검토하는 최후순위 과제입니다.*
- **Google / Microsoft OAuth 사내 간편 로그인 연동 (SSO)**:
  - 구글 워크스페이스나 M365 도메인을 사용하는 기업들을 위해 이메일 주소 대조 기반 OAuth 로그인을 플러그인(인터페이스 분리) 형태로 제공하는 방안을 장기 보류 과제로 둡니다.
- **Redmine 등 사내 외부 협업 도구 연동**:
  - Redmine API를 이용해 사용자 정보 동기화 및 패스워드 인증을 수행하는 확장 모듈을 플러그인 형태로 검토하되, 범용성이 제한되므로 최후순위로 미룹니다.
- **로그인 무차별 대입(Brute-Force) 방어 및 계정 잠금 정책 (패스워드 해싱 비용 튜닝 포함)**:
  - 폐쇄망 단독 구동 시에는 무겁고 불필요할 수 있으므로, 외부망 연동이나 외부 노출 시나리오가 수반되는 특수 환경 요구 과제로 편입하여 검토합니다. 단시간 로그인 오시도에 대한 IP 접속 속도 제한(Rate Limiting) 및 계정 임시 잠금 처리와 함께, 저사양 포터블 PC 환경에서 다중 로그인 시 발생하는 CPU 병목을 제어할 수 있도록 해싱 알고리즘(bcrypt)의 rounds 강도를 유연하게 튜닝할 수 있도록 설정 변수화하는 방안을 포함합니다.
- **외부 보안 표준 대응을 위한 프록시(Nginx/Caddy) 연동 가이드 및 충돌 보완 (Docker 템플릿 포함)**:
  - 포터블 자체 HTTPS 내장은 용량 비대화와 포트 통제 권한 오류를 유발하므로 배제하고, Nginx/Caddy 등 표준 프록시를 연동하는 정석 가이드 및 Docker 배포 시 즉시 활용할 수 있는 설정 파일 템플릿(infra/docker)을 제공하되 프록시 통과 시 로그인 쿠키 유실(Secure/SameSite 헤더 미일치) 및 접속 IP 식별 실패(X-Forwarded-For 미인식) 충돌을 백엔드에서 가볍게 흡수해 줄 수 있는 보완 패치를 최후순위로 검토합니다.
- **실시간 웹소켓 알림**:
  - 일반적인 사용 환경 및 폐쇄망에서는 필요성이 낮으므로, 향후 외부망 연동 등 대규모 업데이트 로드맵이 그려지는 시점까지 검토를 보류합니다.
