# 향후 개선계획 (Product Roadmap)

이 문서는 SHIM 프로젝트의 중장기 발전 방향과 차기 버전의 백로그를 관리합니다. 

---

## 🎯 1. 개발 및 운영 원칙
- **Minimalism**: 폐쇄망 환경의 특수성을 고려하여 외부 의존성을 최소화합니다.
- **Consistency**: 기존의 시간 정책(30/60/120분 단위)과 Dense UI 디자인 시스템을 유지합니다.
- **Verification**: 모든 기능 추가 시 `tools/scripts` 내 검증 시나리오를 동기화합니다.

---

## 🚀 2. 주요 개선 항목 (Backlog)

### [P2] 편의 및 확장 과제
- **통계 리포트**: 전사/팀별 연차 사용 현황 엑셀 다운로드 기능 (관리자용)
- **알림 강화**: 외부망 차단 환경 내에서 브라우저 Web Push 또는 자체 대시보드 알림 위젯
- **접근성 및 UI**:
    - Dense UI 내 고대비 모드 및 키보드 내비게이션 보강
    - 모바일 환경에서의 연차 신청 UX 최적화 (날짜 선택기 개선)
    - **소수점 연차 잔여량 표기 개선**: 30분 단위(0.5시간) 정책 하에서 반올림으로 인해 발생할 수 있는 캘린더/대시보드의 연차 잔여량 소수점 왜곡 현상 개선 (`utils.hours_to_days_hours_compact` 내 정밀도 포맷 보완)
- **사원 비활성화 시 미래 연차 건의 자동 처리**: 관리자가 사원을 비활성화(`is_active = False`)할 때, 해당 사원의 오늘 이후 미래 연차 신청 건(`PENDING`/`APPROVED`)을 자동 취소/반려하여 부서별 캘린더 및 연차 통계 왜곡 방지

---

## 🏗️ 3. 기술 부채 및 아키텍처 과제
- **데이터베이스 및 트랜잭션**:
    - Alembic 도입을 통한 정식 데이터베이스 스키마 버전 관리
    - `AuditLogs` 테이블의 `old_data`, `new_data`를 JSON 포맷으로 전환하여 상세 쿼리 지원
    - **DB 쓰기 트랜잭션 롤백 보장**: 결재 승인/반려 등 일부 DB 쓰기 엔드포인트에서 `db.commit()` 실패 시 예외 처리 및 `db.rollback()`을 보장하도록 구조 개선 (세션 오염 및 좀비 트랜잭션 방지)
    - **외래 키 제약 조건(Foreign Key Constraint) 강제화**: SQLite 연결 시 `PRAGMA foreign_keys = ON;` 프리그마를 활성화하여 물리 데이터베이스 계층의 참조 무결성을 보장
    - **SQLite WAL 모드 백업의 데이터 정합성 보장**: 기존 `shutil.copy2` 단순 복사 방식에서 `sqlite3` 모듈 내장 `backup()` API(온라인 핫 백업)를 사용하여 활성 트랜잭션이 분리된 WAL 모드에서도 무결한 백업 스냅샷을 생성하도록 개선
- **성능 최적화**:
    - PyInstaller 빌드 속도 개선 및 실행 바이너리 용량 최적화 (포터블 최적화)
    - **다중 워커 캐시 불일치 방지**: 다중 프로세스(`uvicorn --workers > 1`) 구동 배포 환경에서 인메모리 전역 설정 캐시 동기화 보완 (TTL 적용 혹은 최종 DB 갱신 시각 검증 기법 도입)
- **보안 및 인증**:
    - 세션 만료 정책 강화 및 중복 로그인 방지 옵션
    - FastAPI Dependency를 활용한 보다 엄격한 RBAC(Role-Based Access Control) 구조 공통화
    - **JWT 쿠키 보안 강화**: Access Token 쿠키 생성 시 `SameSite="Lax"` 명시 및 HTTPS 대응 옵션(`Secure=True`) 제공
- **컨테이너 환경 개선**:
    - **타임존 설정(TZ) 대응**: Docker 배포 시 기본 UTC 환경으로 기동되어 연차 생성 시간(`created_at`) 및 오디트 로그 시간대 오차가 발생하는 현상 해결 (`TZ=Asia/Seoul` 대응 또는 파이썬 내부 타임존 설정 자동화)
- **테스트 커버리지**: 비즈니스 로직(연차 계산)에 대한 단위 테스트 강화 및 CI 자동화 검증





