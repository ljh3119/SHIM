# 작업 로그

비릴리즈성 내부 메모만 남기는 문서입니다. 릴리즈 증적과 검증 결과는 `docs/2-1_운영_릴리즈_통합_산출물.md`, 공개 변경 이력 요약은 `README.md`에서 관리합니다.

## 2026-04-29

- 폴더 구조 표준화/마이그레이션
  - `app/templates/static` → `src/app`, `src/templates`, `src/static`
  - 배포/실행 설정 → `infra/docker`
  - 개발 보조 스크립트 → `tools/scripts`
  - 런타임 데이터 기본 경로 → `var/data`
  - 산출물 → `artifacts/dist`, `artifacts/build` ( `artifacts/build_latest` 정리 )
  - 포터블 spec/테스트 파일 → `portable/SHIM_Portable.spec`, `tools/scripts/test.py`
- 코드/설정/문서 경로 반영 및 호환성 보강
  - `src/app/main.py` 런타임 리소스 경로 탐색 보강 (비포터블 기준 `src` 우선)
  - `package.json`, `tailwind.config.js`, Docker/Compose, PyInstaller spec 경로 갱신
- 릴리즈 동반 검증 결과는 `docs/2-1_운영_릴리즈_통합_산출물.md`에 통합

## 2026-05-01

- 버전 상향 정리
  - 프로그램/템플릿/패키지 버전을 `1.1.7`로 동기화(`src/app/main.py`, `src/templates/base.html`, `package.json`)
  - Docker 기본 이미지 태그를 `shim:1.1.7`로 상향(`infra/docker/docker-compose.yml`, `infra/docker/docker-compose.dev.yml`)
  - 포터블 문서 최신 반영 버전/일자를 `v1.1.7`, `2026-05-01`로 갱신(`portable/README_PORTABLE.md`)
- 산출물 갱신
  - Docker 이미지 `shim:1.1.7`/`latest` 빌드 완료 및 compose 재기동 후 `shim -> shim:1.1.7` 실행 확인
  - 포터블 재빌드 후 `dist/SHIM_Portable` 산출물 확인, 전달 경로 `artifacts/dist/SHIM_Portable`에 최신 `SHIM_Portable.exe`, `_internal` 동기화
- 검증 메모
  - Docker 컨테이너 startup 로그 정상 확인
  - `performance_rehearsal.py` 1회 스모크(`admin_dashboard`, `admin_timeline_all`, `admin_calendar_month`, `admin_calendar_year`) PASS
  - 공식 릴리즈 증적은 `docs/2-1_운영_릴리즈_통합_산출물.md`에 기록

## 2026-05-03

- 버전 릴리즈 **`1.2.0`** — `release.ps1`·`verify_version_sync.ps1` 경로로 코드·Compose·공개 문서 문자열 동기화
- 브랜딩 및 스키마: `SystemSettings` 브랜딩 필드·마스터 저장, `ensure_sqlite_system_schema()`, `product_user_sidebar_title` 제거, 템플릿 컨텍스트/middleware
- UI/레이아웃: 일반 사용자 사이드바 미포함, 관리자 사이드바·dense shell·타임라인/사원 화면 정렬 및 반려 사유 표시, 로그인 `app_version` fallback 정합(`login.html`)
- 레거시 정리(미배포 기준): 미사용 `YearlyLeaveFinalizations` 모델, `src/app/schemas.py`, `partials/sidebar_user.html`, `brand_initial_mark` 컨텍스트 제거
- 사용자 스키마 단순화(미배포 기준): `users.department` 및 관련 fallback/백필 로직 제거, `company`/`team` 기준으로 템플릿·관리 API·설계서 동기화
- 공휴일 시드에 노동절(5/1) 반영 방식 확정(KR 최초 시드 경로 내)
- 산출물: `README.md`, `docs/2-1_운영_릴리즈_통합_산출물.md`, `docs/4-1_…`, `1-1`/`1-3`, `3-1`/`3-2`, `portable/README_PORTABLE.md` 갱신
- 코드 품질: `main.py` 노동절 표시명 유니코드 이스케이프 저장(`\ub178\ub3d9\uc808`). KR 공휴일 시드 감사 로그 `new_data`는 Docker(Linux) 빌드 인코딩 이슈 방지를 위해 ASCII 문자열(`KR holiday seed incl. May 1 Labor Day`)로 통일
- 검증 실행: Docker `shim:1.2.0` 빌드·compose 재기동·컨테이너 내부 `/docs` 200, 로컬 일시 포트 `performance_rehearsal` 4종 PASS, 포터블 `build_portable.ps1` 재빌드(`artifacts/dist/SHIM_Portable`; 스크립트에 CSS 직후 `release.ps1` 동기화 단계 추가)

## 2026-05-07

- 버전 상향: `tools/scripts/release.ps1 -Version 1.2.1` 실행으로 코드/템플릿/compose 기본 태그 동기화
- 가독성 보정: dense 텍스트/경계선 대비 토큰 상향(`dense-muted`, `dense-nav-text`, `dense-line`), 캘린더/공통 하드코딩 색상 정리
- 관리자 사이드바: 토글 버튼을 chevron SVG로 교체하고 버튼 위치/크기/밝기 조정
- 관리자 대시보드: 승인 워크플로우 OFF 시 `결재 대기 n건` 배지 + `결재 대기 건수` KPI 카드 비노출, 그리드 3열 자동 조정

## 2026-05-09 (오늘)

- 프로젝트 리브랜딩 완료: `edu_LAMS` → `SHIM` (Smart Holiday & Integrated Management) 명칭 일괄 전환
- 버그 수정: `portable/build_portable.ps1`에서 산출물 복사 경로가 `LAMS_Portable`로 하드코딩되어 있던 문제를 `SHIM_Portable`로 수정
- UI 브랜딩 강화:
  - 하단 푸터 텍스트를 "Smart Holiday & Integrated Management"로 변경 (`base.html`)
  - 로그인 및 대시보드 내 "LAMS" 표기를 "SHIM" 및 "쉼"으로 교체 완료
- 빌드 정합성:
  - 최종 빌드(Portable, Docker)에서 테스트 데이터가 포함되지 않도록 `var/data/*.db*` 삭제 및 `.dockerignore` 설정 보강
  - 포터블 버전(`artifacts/dist/SHIM_Portable`) 및 Docker 이미지(`shim:1.2.1`) 재빌드 완료
## 2026-05-12 (오늘)

- 버전 릴리즈 **`1.3.0`** — 역할 기반 권한 체계(RBAC) 및 팀 협업 강화
- 데이터 모델 확장:
    - `Users` 모델에 `role` (STAFF/TEAM_LEAD/PM/ADMIN), `position` 필드 추가
    - `SystemSettings` 모델에 `team_calendar_visible` 필드 추가
    - SQLite 컬럼 추가 마이그레이션(`ensure_sqlite_system_schema`) 보강
- 권한 체계 및 자동 마이그레이션:
    - 기동 시 기존 `is_admin=True` 사용자를 `ADMIN`으로 자동 변환 로직 구현
    - 로그인 후 역할별 대시보드 리디렉션 로직 개선
- 팀장 결재 워크플로우:
    - `TEAM_LEAD` 권한자가 본인 팀원의 `PENDING` 건을 승인/반려할 수 있는 API 및 대시보드 위젯 추가
    - 셀프 결재 방지 및 타 팀 결재 차단 보안 로직 적용
- 팀 캘린더 공유:
    - 대시보드 내 동일 팀원의 월별 휴가 현황 매트릭스 뷰 구현
    - 관리자 마스터 설정에서 공유 여부 토글 제어 기능 추가
- 관리자 UI 개선:
    - 사용자 목록에 역할 배지 및 직급 표시
    - 신규 사용자 등록 시 역할/직급 지정 지원
- 산출물 업데이트:
    - `README.md`, `docs/2-1_…`, `docs/3-1_…`, `docs/3-2_…`, `docs/4-1_…`, `portable/README_PORTABLE.md` 최신화
- **프로젝트 최종화 및 레거시 정리 (2026-05-12)**:
    - 정식 출시를 앞두고 불필요한 레거시 코드 및 데이터 정리 수행
    - `src/app/main.py` 내 스키마 보강(`ensure_sqlite_system_schema`), 레거시 마이그레이션(`migrate_legacy_leaves_to_snapshot`), `is_admin` 역할 변환 로직 제거
    - `design/ui-handoff/legacy` 폴더 및 `build`, `dist` 등 빌드 파생물 제거
    - 데이터베이스 초기화(과거 데이터 삭제 및 신규 기동 준비 완료)
    - 설계서 및 가이드 문서 내 `edu_LAMS` 잔여 명칭 `SHIM`으로 일괄 교체

## 2026-05-13 (오늘)

- 버전 상향 릴리즈 **`1.3.1`**
- 의존성 및 엔진 업그레이드:
    - Tailwind CSS v4 통합 및 `@tailwindcss/cli` 도입
    - 패키지 의존성(`tailwindcss`, `@tailwindcss/cli`)을 `v4.3.0`으로 최신화
- 결재 시스템 및 사용자 UI 고도화:
    - 결재 가능 역할 확장: `TEAM_LEAD` 외에 `PM`(프로젝트 매니저) 역할도 결재 가능하도록 로직 반영
    - 전용 결재 관리 페이지(`user_approvals.html`) 신설 및 라우터(`user/approvals`) 추가
    - 대시보드 메뉴 분리: 복잡한 대시보드를 `결재 관리`, `우리 팀 캘린더`, `나의 신청 이력`으로 분할하여 UI 최적화
    - 연차 신청 로직 수정: 반려(`REJECTED`)된 건이 시간대 점유를 하지 않도록 중복 체크 로직 보정
- 빌드/배포 정합성:
    - `release.ps1`을 통한 `package.json`, `main.py`, `base.html`, `docker-compose` 버전 동기화
    - `npm run build:css` (Tailwind v4) 빌드 검증 및 `verify_version_sync.ps1` 통과 확인
    - `README.md`, `portable/README_PORTABLE.md`, `docs/2-1_…` 등 공개 문서 및 산출물 버전 정보 일괄 갱신

- 버전 상향 릴리즈 **`1.3.2`** (긴급 개선 및 UI 최적화)
- 기능 개선:
    - PM 자동 승인: PM 역할의 사용자가 연차 신청 시 시스템 설정과 무관하게 즉시 `APPROVED` 상태로 저장되도록 로직 수정 (`api_user.py`)
- UI/UX 최적화:
    - 사용자 대시보드 통계 추가: `user_dashboard.html` 상단에 총 연차, 사용 시간, 잔여 시간, 신청 건수 요약 카드 이식
    - 팀 캘린더 필터 구조 개선: `user_team_calendar.html`의 연도/월 조회 필터를 별도의 상단 박스 패널로 분리하여 조작성 향상
- 검증 및 정합성:
    - `verify_pm_logic.py` 테스트 스크립트를 통한 PM 자동 승인 로직 검증 완료
    - `release.ps1`을 이용한 전사 버전 동기화(v1.3.2) 및 CSS 재빌드 완료

- 검증:
    - `python -m compileall` 통과
    - Tailwind v4 기반 CSS 정상 생성 확인


