# 시스템 작업 로그 (System Internal Logs)

이 문서는 공식 릴리즈 증적에 포함되지 않는 개발 단계의 내부 기술 메모 및 작업 이력을 기록합니다. 주요 기능의 아키텍처적 결정이나 문제 해결 과정 등을 보관하여 향후 유지보수의 참고 자료로 활용합니다.

---

## 📅 2026-04-29
### 폴더 구조 표준화 및 자산 이동
- **Backend**: `app/` -> `src/app`
- **Frontend**: `templates/`, `static/` -> `src/templates`, `src/static`
- **Infrastructure**: Docker 관련 설정을 `infra/docker`로 통합
- **Scripts**: 개발 보조 도구들을 `tools/scripts`로 이동
- **Data**: 런타임 데이터 경로를 `var/data`로 표준화

---

## 📅 2026-05-01
### 버전 동기화 체계 구축
- **버전 일치**: `main.py`, `base.html`, `package.json` 버전을 `1.1.7`로 동기화
- **Docker**: 이미지 태그 정책 수립 (`shim:version` 및 `latest`)
- **Verification**: `performance_rehearsal.py`를 통한 초기 부하 테스트 수행 (All PASS)

---

## 📅 2026-05-03
### v1.2.0 릴리즈 준비
- **Branding**: `SystemSettings`를 통한 동적 리브랜딩 로직 구현
- **UI**: Dense UI 디자인 시스템의 1차 반영 (관리자 shell 및 주요 테이블)
- **Legacy Cleanup**: 미사용 모델(`YearlyLeaveFinalizations`) 및 스키마 파일 제거

---

## 📅 2026-05-09
### 프로젝트 리브랜딩 (`edu_LAMS` -> `SHIM`)
- **명칭 전환**: 시스템 전반의 명칭 및 폴더 구조를 `SHIM`으로 일괄 전환
- **Build Fix**: 포터블 빌드 스크립트 내 경로 하드코딩 결함 수정
- **Sanitization**: 빌드 산출물 배포 전 `var/data/*.db` 자동 삭제 로직 추가

---

## 📅 2026-05-12
### v1.3.0 RBAC 및 보안 강화
- **Model**: `Users.role`, `Users.position` 필드 추가 및 데이터 마이그레이션
- **Security**: 팀장급 결재 권한 격리 로직 및 셀프 결재 방지 기능 구현
- **Cleanup**: 정식 출시 전 레거시 `is_admin` 필드 호환 로직 제거

---

## 📅 2026-05-13
### v1.3.1/v1.3.2 긴급 패치 및 UI 고도화
- **UI Engine**: Tailwind CSS v4 전환 및 `@tailwindcss/cli` 도입
- **Fix**: 반려된 신청 건의 시간 점유 결함 수정
- **Feature**: PM 자동 승인 로직 반영 및 사용자 대시보드 KPI 카드 추가
- **Refactoring**: 메인 `README.md` 및 `docs/` 산출물 전면 리팩토링 (Technical Writing 적용)

---

## 📅 2026-05-14
### v1.3.5 아키텍처 최적화 및 레거시 제거
- **Architecture**:
  - `src/app/utils.py` 신설 및 라우터(`api_admin`, `api_user`) 내 중복 헬퍼 함수 통합.
  - `src/app/services/admin_service.py` 신설하여 라우터의 거대 비즈니스 로직(대시보드 통계, 타임라인 쿼리) 이관.
- **Legacy Removal**:
  - 전역 코드베이스에서 `is_admin` 필드 의존성 제거 및 `role == 'ADMIN'` 기반 권한 체계로 일관화.
  - 템플릿 내 `u.is_admin` 참조 구문 수정.
- **Cleanup**: 비어 있는 `src/templates/samples/` 폴더 제거.
- **Verification**: 전용 검증 스크립트(`verify_refactoring.py`)를 통한 로직 정합성 확인.

---

## 📅 2026-05-18
### 시스템 안정화 및 핵심 로직 복구 (v1.4.0 패치)
- **핵심 로직 복구**: 
  - 리팩토링 과정에서 누락되었던 사용자의 연차 신청(`POST /user/leave`) 엔드포인트 복구.
  - v1.4.0 설계에 맞춰 **PM 자동 승인**, **정밀 시간 차감(점심 제외)**, **중복 신청 방지** 로직을 포함하여 재구현.
- **검증 체계 고도화**:
  - `run_remaining_tests.py`를 v1.4.0 기준으로 전면 개편하여 PM 권한 및 삭제 감사 로그를 자동 검증하도록 개선.
  - SQLite "Database is locked" 이슈 해결을 위한 테스트 세션 관리 로직 최적화.
- **테스트 환경 강화**:
  - 데모 및 수동 테스트를 위한 풍부한 조직 데이터를 생성하는 `seed_test_data.py` 스크립트 신설.
- **빌드 및 배포**:
  - 복구된 로직을 포함하여 Docker 이미지 및 포터블 64-bit 패키지 재빌드 완료.

---

## 📅 2026-05-18
### v1.4.1 공가/출장(연차 미소모) 기능 확장 및 관리자 정정 도입
- **DB 스키마 확장**: `Leaves` 모델에 `is_deductive`(차감 여부), `reason`(신청 사유) 필드 추가 및 SQLite 마이그레이션 적용.
- **백엔드 로직 고도화**:
  - 연차 차감 건만 잔여 연차 및 통계 로직에서 계산되도록 필터 강화 (`is_deductive=True`).
  - 공가/출장 신청 시 사유 입력 필수화 및 동일 시간대 중복 신청 방지 로직 보강.
  - 관리자 전용 `POST /admin/leave/update-type` 엔드포인트 신설 (연차 ↔ 공가/출장 즉시 전환 및 감사 로그 연동).
- **UI/UX 개선 (Dense UI 유지)**:
  - 캘린더 전역에서 공가/출장을 보라색 배지 및 '0.0h' 포맷으로 통일하여 시각적 직관성 제공.
  - 신청 내역 탭에서도 팀 캘린더 및 결재 관리 메뉴 내비게이션 일관성 유지.
  - 관리자/팀장 타임라인 및 결재 목록에서 공가/출장 여부와 사유를 촘촘하게(Dense) 배치.
  - PM 프로젝트 캘린더에 동적 정렬(이름, 팀, 잔여, 사용량) 및 상세 포맷팅(X일 Yh) 적용.

---

## 📅 2026-05-20
### v1.4.2 캘린더 공유 범위 통합 관리 및 공가/연차 전환 안정화 (Hotfix)
- **캘린더 공유 범위 설정 통합 관리 (UX 개선)**:
  - `admin_master.html`에 산재되어 있던 개별 토글 형태(팀 캘린더 온/오프, 전사 캘린더 온/오프)를 하나의 단일 UI 설정 카드(3선택 라디오: 공유 안 함 / 소속 팀원 공유 / 전사 공유)로 개편.
  - 백엔드에 `POST /admin/settings/calendar-scope` 엔드포인트를 구현하여 일괄 적용 및 `UPDATE_CALENDAR_SCOPE_SETTING` 단일 감사 로그 연동. 불필요한 레거시 개별 토글 API 2개 제거.
  - 일반 사원의 팀 캘린더 접근 권한을 3선택 옵션 상태와 유기적으로 매핑하여 `none` 선택 시 URL 직접 접근을 대시보드로 `302 Redirect` 시키는 제어장치 마련.
- **공가 ↔ 연차 유형 전환 에러 조치 (Hotfix)**:
  - 타임라인 등에서 휴가 건의 유형을 전환할 때(`POST /admin/leave/update-type`) 잔여 연차 조회에 쓰이는 `resolve_user_yearly_allocated_hours` 함수 및 SQLAlchemy `func` 객체의 임포트 누락으로 인해 `NameError` 및 500 에러가 발생하던 문제 조치.
- **테스트 및 검증 자동화**:
  - `run_remaining_tests.py` 내 `[CASE 3]` 시나리오에 공가 전환 후 연차 복구 전환 기능 검증 단계를 추가하여 재발 방지.
  - `[CASE 5]` 시나리오를 새로운 통합 API `/admin/settings/calendar-scope` 및 리다이렉트 권한 격리 상황을 완벽하게 모의 검사하도록 고도화.
  - `seed_test_data.py` 스크립트를 통한 데이터베이스 리셋 및 시딩 완료.
- **버전 정합성 동기화**:
  - `package.json` 버전을 `1.4.2`로 기입하고 `release.ps1` 동기화 확인. `README.md` 및 `portable/README_PORTABLE.md` 내에 기재된 릴리즈 버전 명세를 일치시켜 배포 체크 검증 통과.

---

## 📅 2026-05-21
### v1.5.0 다수일 일괄 신청 기능 구현, UI/UX 개선 및 성능 최적화
- **다수일 일괄 신청 백엔드 기능 개발**:
  - `POST /user/leave` 엔드포인트에서 쉼표(`,`)로 구분된 다중 날짜 입력을 파싱하여 처리하는 기능 구현.
  - 데이터베이스 트랜잭션 블록 내에서 루프를 돌며 개별 날짜별 검증(중복 시간대, 1일 8시간 한도 초과 등)을 수행하고, 정책 위반 시 전체 변경 사항을 롤백(Rollback)하도록 원자성 보장.
  - 주말(토, 일) 및 데이터베이스에 등록된 공공 공휴일은 자동으로 신청 대상에서 제외.
  - 일괄 처리 완료 시 상세 정보를 포함하는 단일 감사 로그(`APPLY_LEAVE_BULK` 액션) 적재 연동.
- **사용자 대시보드 및 연차 캘린더 UI/UX 개선**:
  - 대시보드 직접 신청 패널에 "단일 날짜" / "기간 선택 (다수일)" 모드 전환 탭 디자인 추가 및 날짜 분기 입력창 연동.
  - 연차 캘린더 헤더에 "다중 선택 신청 모드" 토글 버튼 탑재 및 날짜 클릭 제어 스크립트 작성.
  - 화면 하단에 고정되는 세련된 플로팅 바("선택한 {N}개 날짜 일괄 신청", "선택 취소") 마크업 및 애니메이션 연동.
  - 신청 모달에 선택된 날짜들을 가로 칩 리스트 형태로 동적 시각화하여 사용자가 한눈에 확인할 수 있도록 개선.
- **시스템 성능 최적화 (인메모리 캐싱 및 N+1 쿼리 제거)**:
  - `SystemSettings` 조회 부하를 감소시키기 위해 `leave_policy.py` 계층에 인메모리 캐시 메커니즘(`_cached_settings` 및 `get_system_settings`, `invalidate_settings_cache`) 구현.
  - 관리자 설정 업데이트 API (`api_admin.py` 등) 완료 시 캐시를 강제로 무효화하여 데이터 무결성 보장.
  - 관리자 캘린더 API `/admin/leaves/calendar`에서 사원(N명) 조회 루프 내 개별 `Leaves` 테이블 쿼리를 사원 ID 목록 기반의 Bulk IN 쿼리로 최적화하여 쿼리 횟수를 1회로 고정.
- **통합 검증 자동화**:
  - `run_remaining_tests.py`에 `[CASE 6]`(다수일 일괄 신청 및 롤백 원자성 검증)과 `[CASE 7]`(설정 캐시 무효화 및 데이터 정합성 검증) 시나리오 테스트 코드를 추가하여 전체 통합 검증 완료 (**All 7 Cases PASS**).
- **버전 동기화**:
  - `release.ps1`을 실행하여 애플리케이션 버전 명세를 `1.5.0`으로 상향 및 동기화 수행.

---

## 📅 2026-05-21
### v1.5.1 시간 선택 생략 및 하루 종일(Full-day) 신청 자동화
- **백엔드 API 개선 (`POST /user/leave`)**:
  - `start_time` 및 `end_time` 파라미터에 기본값 `""`을 부여하여 시간 선택을 생략 가능하도록 호환성 수정.
  - 시간 생략(빈 문자열) 수신 시 전사 설정(`SystemSettings`)에서 조회한 기본 근무 시작/종료 분(예: 540, 1080)을 `"HH:MM"` 문자열로 자동 변환하여 연차 계산기에 전달함으로써, 개별 날짜별 8시간(점심시간 제외) 하루 종일 신청으로 자동 처리되도록 고도화.
- **프론트엔드 대시보드 UI/UX 개선**:
  - 단일 날짜 신청 영역 내에 '하루 종일' 체크박스(`direct_all_day`) 탑재 (기본값: 체크 해제). 체크 여부에 따라 시간 선택 UI가 슬라이드/토글되어 인지적 복잡성 최소화.
  - '기간 선택(다수일)' 및 '캘린더 다중 선택 신청' 시, 시간 입력 UI를 화면에서 원천 차단하고 "선택한 날짜들에 대해 하루 종일(Full-day) 신청으로 등록됩니다." 힌트 문구를 상시 표출하여 불필요한 마우스 동선 제거.
  - 캘린더 다중 신청 및 직접 신청 제출 스크립트 수정: 시간 생략 모드 시 기존 시간 포맷 검증 단계를 우회하고 컨펌창을 거쳐 즉시 제출되도록 JavaScript 밸리데이터 최적화.
- **통합 검증 자동화**:
  - `run_remaining_tests.py` 내 `[CASE 6]` 시나리오 하단에 시간 생략 전송 자동 검증 시나리오(단일 하루종일 체크박스 전송, 기간 시간 생략 전송)를 확장하고, 통합 검증 러너 작동 결과 **All 7 Cases PASS** 확인 완료.

---

## 📅 2026-05-21
### v1.5.2 일반 사용자 및 관리자 비밀번호 자가 변경 기능 복구 및 개선
- **일반 사용자 비밀번호 자가 변경 모달 및 API**:
  - `base.html`에 사용자 전용 3필드(현재 비밀번호, 새 비밀번호, 새 비밀번호 확인) 모달(`pwdModal`) 추가.
  - `/user/change-password` 백엔드 API 구현하여 현재 비밀번호 일치 검사, 새 비밀번호 4자 이상 강제화, 암호화 처리 적용.
- **관리자 비밀번호 변경 폼 통일**:
  - `adminPwdModal` 폼에 '새 비밀번호 확인' 필드를 적용하여 3필드로 개선 및 클라이언트 정합성 검증 보강.
- **로깅 보안 강화 및 마스킹**:
  - 비밀번호 변경 시 생성되는 감사 로그(`CHANGE_PASSWORD`, `CHANGE_ADMIN_PASSWORD`) 내 `old_data`, `new_data`를 `"*****"`로 완전 마스킹하여 DB 적재 처리.
- **릴리즈 및 테스트 자동화**:
  - `run_remaining_tests.py`에 `[CASE 8]` 추가하여 모든 예외 및 검증 시나리오 통과 확인 (All 8 Cases PASS).
  - `release.ps1` 스크립트를 통해 전체 버전 일관성 검증(`verify_version_sync.ps1`) 및 Tailwind CSS 빌드 등 릴리즈 배포 검증 완료.
- **컨테이너 환경 타임존(TZ) 불일치 해결**:
  - `Dockerfile`에 `ENV TZ=Asia/Seoul`을 명시하여 기본 타임존을 한국 표준시(KST)로 지정.
  - `docker-compose.yml` 및 `docker-compose.dev.yml`에 `TZ: Asia/Seoul` 환경 변수를 주입하여 컨테이너 환경 전반에서 시간 생성 및 로깅 시 9시간 오차가 발생하지 않도록 해결.

---

## 📅 2026-05-21
### 감사 로그 한글화 개선 (Localization Enhancement)
- **액션 라벨 매핑 추가**:
  - `APPLY_LEAVE_BULK` (다수일 연차 일괄 신청) 및 `UPDATE_LEAVE_TYPE` (휴가 유형 정정) 액션 코드에 대해 한글 번역 명칭을 `AUDIT_ACTION_LABELS`에 추가 반영.
- **대상 정보(Target Info) 파싱 고도화 및 공용화**:
  - `get_audit_action_label`, `get_audit_target_label` 함수를 신설하여 감사 로그의 수행 액션과 대상 정보(사원, 관리자, 공휴일, 연차신청, 일괄처리 범위 등)를 자연스러운 한국어로 변환하도록 로직 통합.
  - 웹 화면 조회용 라우터(`admin_audit_logs`)와 엑셀 다운로드용 라우터(`admin_audit_export`)가 공용 헬퍼 함수를 동일하게 호출하도록 일원화하여 표현 및 동작 일관성을 확보.
- **정적/동적 테스트 확인**:
  - 테스트 및 엑셀 다운로드 시나리오 검증 완료.

### 사용자 대시보드 및 관리자 타임라인 UI/UX 개선
- **관리자 타임라인 가로 스크롤 제거**:
  - 표 헤더/데이터 셀 패딩(`px-3` -> `px-2`), 수직 패딩(`py-3` -> `py-2.5`) 및 열 너비를 최적화하여 데스크톱 표준 해상도에서 가로 스크롤바가 생기지 않도록 차단.
  - 상태 컬럼 셀 크기를 줄이고 내부 select 태그에 `w-full` 부여.
  - "공가/출장 전환" 버튼의 명칭을 "공가 전환"으로 간소화하여 너비 절약.
  - 표에 데이터가 없는 경우 렌더링되는 행의 `colspan` 누락 버그(8 -> 9) 수정.
- **하루종일 힌트 박스 통일성 강화**:
  - 대시보드 직접 신청 패널 및 캘린더 다중 신청 시 표시되는 하루종일 안내 점선 힌트 상자를 레이블("사용 시간대")을 포함한 래퍼 영역으로 감싸고 `min-h-[38px]`와 패딩 조정을 적용하여 기존 시간 드롭다운들과 높이/위치 정렬을 맞춤.
  - JS 내 안내 영역 표시/숨김 변수 ID 타겟을 힌트 래퍼 ID로 수정.
- **날짜 입력창 스타일 일원화**:
  - 기간 선택의 시작일/종료일 날짜 입력 필드의 크기/패딩 스타일을 단일 날짜 필드와 동일하게 `text-sm px-3 sm:text-sm`으로 일원화하여 이질감 해소.

---

## 📅 2026-05-22
### v1.5.5 기술 부채 해결 및 시스템 안정성/보안/포터블 빌드 최적화 (v1.5.5 릴리즈)
- **SQLite WAL 백업 안정화**:
  - `src/app/services/ops.py`의 `create_sqlite_backup` 함수에서 활성 세션 상태의 무결한 복제를 보장하는 `sqlite3.backup()` API를 적용하여 WAL 분리 트랜잭션 유실 리스크 극복.
- **SQLite 쓰기 락 방지 timeout 튜닝**:
  - `src/app/database.py`에서 SQLAlchemy `create_engine` 구동 매개변수 `connect_args`에 `timeout: 30.0`을 지정하여 락 대기 오버플로우로 인한 예외 차단.
- **PyInstaller 배포 본체 용량 감축**:
  - `portable/build_portable.ps1`의 PyInstaller 패키징 스크립트에 `--exclude-module PIL`, `--exclude-module watchfiles`, `--exclude-module setuptools`를 선언하여 빌드 본체 용량 축소 적용.
- **문서 동기화**:
  - `docs/1-1_초심자_구동_가이드.md` 및 `docs/1-3_운영_릴리즈_실행_체크리스트.md` 내 예시 빌드 태그 및 버전을 `1.5.5`로 갱신.
  - `docs/3-1_향후_개선계획.md`에서 완료 항목 제거 및 비동기 라우터 리스크를 기획 검토/방향성 고민 과제로 추가 기술.
- **최종 검증**:
  - 통합 회귀 테스트 `run_remaining_tests.py` 가동 및 릴리즈 일괄 검증 체크 (`release.ps1`) 정상 완료.

### v1.5.3 캘린더 미결재 연차 렌더링 개선 및 UI 가독성 고도화
- **사용자 대시보드 안내 문구 개선**:
  - 개인 신청 페이지 하단의 단일 행 안내 메시지를 2개 단락(`div` 컨테이너 내 2개의 `p` 요소)으로 분할하고, 촘촘한 세로 간격(`space-y-0.5`)을 부여하여 정보 가독성을 큰 폭으로 보완.
- **팀 캘린더 공가/출장 대기(PENDING) 스타일 정정**:
  - 결재 대기 중인 공가/출장(비차감) 신청 건의 대기 분기에서 차감 연차 시간 계산 시 중복 누적되던 현상을 해결 (`is_deductive` 조건식 추가 및 `ns_t.has_non_deductive_pending` 플래그 도입).
  - 대기 중인 공가/출장 건도 일반 대기 연차처럼 보라색 계열 바탕 위에 반투명 및 점선 테두리 스타일(`public` + `pending`)이 정상 중첩 적용되도록 수정하여 일반 연차와의 시각적 일관성을 확보.
- **관리자 연차 캘린더 미결재 표시 보완**:
  - 일자별 뷰 렌더링 시 해당 날짜의 연차들 중 결재 대기 중(`PENDING`)인 신청 건이 존재하는 경우 `day-block`에 `pending` 클래스를 동적으로 주입하도록 수정.
  - 내장 스타일시트에 `.day-block.pending` 선택자 규칙(투명도 `0.45` 및 `2.2px dashed` 테두리)을 새로 선언하여 승인 완료된 연차와 미결재 연차가 시각적으로 명확히 구별되도록 해소.
- **통합 검증 스크립트 보강**:
  - `run_remaining_tests.py` 내의 `next_weekday` 함수를 대체공휴일 등 DB 시딩된 공휴일까지 회피하도록 `next_business_day`로 고도화하여 날짜별 테스트 구동 시의 검증 정합성 이슈 사전 제거.
  - `release.ps1 -Version 1.5.3 -RunChecks`를 통한 빌드 및 버전 검증 절차 전원 통과 완료.

---

## 📅 2026-05-22
### 기술 부채 해결 및 시스템 안정성/보안 강화 작업 (v1.5.4 준비)
- **SQLite 외래 키 제약 조건 활성화**:
  - `database.py`에서 SQLite 데이터베이스 연결 이벤트 리스너(`set_sqlite_pragmas`)에 `PRAGMA foreign_keys=ON;` 설정을 추가하여 물리 계층에서의 참조 무결성을 보장하도록 개선했습니다.
- **트랜잭션 롤백 보장 (예외 처리 고도화)**:
  - `api_admin.py`의 핵심 사원/연차 관리 쓰기 작업(사원 생성/수정, 비밀번호 초기화, 활성 상태 토글, 연차 상태 수정) 및 `api_user.py` 내 팀 결재(승인/반려) 처리 엔드포인트의 `db.commit()` 구문에 `try-except SQLAlchemyError` 구문을 일괄 적용했습니다.
  - 이를 통해 데이터베이스 작업 실패 시 `db.rollback()`이 무조건 보장되도록 예외 처리 구조를 고도화하여 트랜잭션 오염을 원천 차단했습니다.
- **JWT 쿠키 보안 강화**:
  - `main.py`의 로그인 API(`/login`)에서 JWT 쿠키를 설정할 때 `SameSite="Lax"` 옵션을 명시적으로 적용했습니다.
  - 또한, 실서비스 환경에서 유연하게 대응할 수 있도록 접속 프로토콜이 HTTPS(또는 프록시 헤더 `x-forwarded-proto`가 `https`)이거나 `SHIM_SECURE_COOKIE` 환경 변수가 활성화되었을 때 자동으로 `Secure=True` 플래그가 주입되도록 구현하여, HTTP 개발 환경 호환성을 동시에 확보했습니다.
- **소수점 연차 잔여량 표기 정밀화**:
  - `src/app/utils.py`의 `hours_to_days_hours_compact` 함수에서 30분 단위(0.5시간)의 소수점 연차가 반올림되어 정수로 뭉개져 캘린더 및 대시보드 화면에 왜곡 렌더링되던 버그를 정정했습니다. 0.5단위 소수점이 소수 첫째 자리까지 온전히 유지되도록 `round(val, 1)` 처리를 적용했습니다.
- **캘린더 대기 연차 테두리 가독성 정정**:
  - 미결재(대기) 상태의 연차에 적용되어 가시적으로 지저분했던 점선 테두리(`border: dashed`)를 완전 제거(`border: none !important;`)하고, 어드민 및 팀원 캘린더(`admin_leaves_calendar.html`, `user_team_calendar.html`) 상에서 통일되게 투명도 대비(`opacity: 0.55`)와 그레이스케일(`filter: grayscale(0.35)`) 조합을 부여하여 디자인 통일성과 가독성을 극대화했습니다.
- **사원 관리 직급명 예시 개편**:
  - `src/templates/admin_users.html` 내 신규 사원 등록 및 정보 수정 폼의 직급명 입력 필드 플레이스홀더를 기존 `예: 선임연구원`에서 `예: 수석`으로 일괄 교체했습니다.
- **검증 프로세스 수행**:
  - `verify_refactoring.py` 단정 검사 스크립트를 `-4.5h` 포맷에 맞춰 최신화하고 테스트를 정상 통과시켰습니다.
  - `run_remaining_tests.py`를 실행하여 8개 통합 시나리오 테스트 전체가 회귀 버그 없이 PASS함을 확인 완료했습니다.

---

## 📅 2026-05-22
### v1.5.6 코어 아키텍처 안정화 (FastAPI 라우터 동기화 및 캐시 제거)
- **FastAPI 라우터 `def` 전면 전환**:
  - `api_user.py`, `api_admin.py`, `main.py` 내의 모든 라우터 함수(총 41개)를 `async def`에서 동기식 `def`로 전면 리팩토링했습니다.
  - 이로써 동기 SQLAlchemy DB를 호출할 때 파이썬 메인 이벤트 루프가 차단되는 병목 요인을 차단하고 스레드풀에서 안전하게 병렬 처리되도록 아키텍처를 안정화했습니다.
- **`branding_middleware` 미들웨어 스레드풀 적용**:
  - 미들웨어는 비동기로 기동해야 하므로, 내부의 동기 DB 조회 로직인 `_load_branding_into_request`를 `fastapi.concurrency.run_in_threadpool`로 래핑하여 메인 루프 블로킹 현상을 완벽하게 방지했습니다.
- **다중 Uvicorn 워커 인메모리 캐시 제거**:
  - `leave_policy.py`에서 `SystemSettings`를 캐싱하던 전역 캐시 `_settings_cache`와 무효화 로직(`invalidate_settings_cache`)을 전면 제거했습니다.
  - 다중 프로세스 워커 배포 환경에서 설정 불일치로 인한 예기치 못한 논리 오류(연차 차감 시간대 오류 등)를 원천 차단하고 항상 DB에서 직접 상태를 조회하도록 아키텍처 상태를 극단적으로 단순화했습니다.
- **버전 및 문서 동기화**:
  - `package.json`, `main.py`, `README.md` 내 버전을 `1.5.6`으로 동기화했습니다.
  - `docs/3-1_향후_개선계획.md`에서 완료된 아키텍처/런처/경량화 백로그를 정리하고 `docs/2-1_운영_릴리즈_통합_산출물.md`에 v1.5.6 상세 내역을 적재했습니다.

### v1.5.6 추가 개발: 통합 검증 테스트 시나리오 보강 및 무결성 검증 (CASE 13~16 추가)
- **통합 검증 스크립트 확장**:
  - `run_remaining_tests.py` 내 기존 12개 시나리오에 신규 검증 시나리오 4종(CASE 13~16)을 완벽히 설계 및 구현하여 총 16개 테스트 케이스의 원클릭 통합 회귀 검증 패스를 확보했습니다.
- **CASE 13: RBAC 권한 우회 차단 검증**:
  - 일반 사원(STAFF) 권한 세션에서 비정상적인 관리자 페이지 접근(302 Redirect/403 Forbidden) 및 타인의 연차 승인 시도에 대해 서버 계층에서 비즈니스 권한 검사로 차단되는 구조를 엄밀하게 검증했습니다.
- **CASE 14: 정밀 시간 차감 및 점심시간 자동 제외 검증**:
  - 다양한 경계 조건(30분, 60분, 120분 시간대 신청 및 오전/오후 단기 연차) 하에서 12:00~13:00 사이의 점심시간이 자동으로 감지 및 공제되어 차감 시간이 계산되는 정밀 엔진의 정확성을 재검증했습니다.
- **CASE 15: 퇴사자(비활성 사용자) 로그인 및 신청 차단 검증**:
  - 관리자에 의해 비활성화(`is_active = False`)된 사용자의 세션 즉시 만료 및 로그인 원천 차단, 그리고 신규 연차 신청 차단 로직이 완벽하게 가동하는 것을 검증했습니다.
- **CASE 16: 수동 공휴일 신청 차단 검증**:
  - 평일 중 하루를 수동으로 공휴일(`Holidays`)로 지정한 뒤 해당 날짜에 연차를 신청했을 때, "공휴일입니다" 라는 안내 팝업 문구와 함께 신청 프로세스가 안전하게 차단되는지 검증했습니다.
- **문서 동기화**:
  - `docs/2-1_운영_릴리즈_통합_산출물.md`에 검증 시나리오 1~16의 요약 및 결과 증적을 통합 갱신했습니다.

### v1.5.7 제로 컨피그 배포 및 DDL 자동 마이그레이션 적용
- **포터블/런처 제로 컨피그 자동화**:
  - `run_portable.bat` 및 `shim_portable.py`에서 포트 충돌을 자동 해결하기 위해 포트 탐색 루프(8000~)를 구현했습니다.
  - `SHIM_Portable.exe` 중복 프로세스 실행 시 기존 인스턴스 브라우저 URL로 연결하고 신규 프로세스는 스킵하는 방지 로직을 수립했습니다.
  - 비밀키 입력 허들을 제거하고, `data/secret.key`가 없을 시 64글자 랜덤 비밀키를 자동 생성 후 영구 로드하는 메커니즘을 적용했습니다.
- **자가 복구 DDL 자동 마이그레이션 엔진 탑재**:
  - Alembic을 완전 배제하고, `migrations.py` 및 `main.py` 기동 시점에 `schema_versions` 테이블을 통해 마이그레이션 이력을 관리하도록 개선했습니다.
  - `PRAGMA table_info` 조회를 기반으로 각 테이블(`users`, `leaves`, `system_settings`)의 신규 컬럼 존재 여부를 확인 후, 누락된 경우에만 안전하게 `ALTER TABLE`을 수행하게 함으로써 기존 수동 수정 디비에서도 에러 없이 스키마가 일관되게 동기화됩니다.
- **마이그레이션 운영/개발 분리 설계 협의**:
  - 정식 릴리즈 시점 이전의 개발용 임시 마이그레이션 이력과 정식 버전 이후의 실운영 마이그레이션을 분리하기 위해 "베이스라인 스키마 통합(Squash)" 정책을 수립하고, 관련 사항을 `docs/3-1_향후_개선계획.md` 및 `docs/4-1_SHIM_프로젝트_설계서.md`에 명문화했습니다.

### v1.5.8 포터블 빌드 최적화 및 의존성 슬림화 (Lightweight Build)
- **포터블 빌드 사양 최적화**:
  - `portable/build_portable.ps1`과 `SHIM_Portable.spec`에 `numpy`, `matplotlib`, `tkinter`, `_tkinter`, `pytest`, `unittest`, `IPython` 등 런타임 구동에 불필요한 대형 서드파티 및 GUI 의존 모듈들을 배제(`excludes` 추가)하여 정합성을 동기화했습니다.
- **최적화 수치 성과**:
  - **바이너리 용량**: 빌드 크기가 약 **173 MB**에서 **49.58 MB**로 약 **71.3%** 대폭 경량화되어 슬림한 배포 본체 기동 성능을 확보했습니다.
  - **빌드 빌드 속도**: 정적 모듈 병합 간소화로 빌드 소요 시간이 약 **145초**에서 **84초**로 약 **42%** 단축되었습니다.
- **통합 검증 및 안정성 확보**:
  - 모듈 배제 후 실제 런타임 동작 및 비즈니스 연산에 미치는 사이드 이펙트 검증을 위해 `run_remaining_tests.py` 통합 회귀 시나리오 16종을 구동하였고, **전원 통과 (All 16 Cases PASS)**를 획득하여 무결성을 보증했습니다.
- **버전 및 문서 동기화**:
  - `package.json`, `main.py`, `base.html` 내 버전을 `1.5.8`로 상향했습니다.
  - `docs/3-1_향후_개선계획.md`에서 완료된 포터블 빌드 최적화 과제를 삭제 정리하고, 차기 릴리즈 및 개발 로그에 이력을 적재했습니다.

---

## 📅 2026-05-22
### v1.5.9 보안 보강 및 API 리팩토링 (P2 과제 완료)
- **공통 권한 검증 및 RBAC 통합 (`Depends` 기반 리팩토링)**:
  - `src/app/dependencies.py`를 신설하여 `get_current_user` 및 `get_current_admin` 선언형 의존성 주입 체계를 구축했습니다.
  - 이를 통해 `api_user.py` 및 `api_admin.py`에서 수백 줄에 달하던 중복 예외 처리와 인증 로직을 제거하고 선언적 보안 경계를 구현했습니다.
- **URL 접두사 기반 분기 처리 예외 핸들러 최적화**:
  - `main.py`에 `NotAuthenticatedException` 및 `PermissionDeniedException` 전역 예외 핸들러를 등록하고, 인증 실패 시 `/api/` 접두사로 시작하는 모든 요청은 `401 Unauthorized` JSON을 응답하며, 그 외의 일반 웹 요청은 일괄적으로 로그인 스크린`/`으로 302 리다이렉트되도록 단일화했습니다.
  - `/static/`, `/docs`, `/openapi.json`, `/favicon.ico` 등의 자원에 대해서는 최상단 통과(Pass) 분기를 설계하여 무한 리다이렉션 루프에 빠지지 않고 안전하게 404/차단되도록 보완했습니다.
- **세션 및 쿠키 보안 속성 정교화**:
  - `src/app/auth.py`에 `get_cookie_settings(request)` 함수를 추가하여 SameSite, Secure, HttpOnly 속성을 일원화했습니다.
  - HTTPS 접속 감지 및 `SHIM_SECURE_COOKIE` 환경 변수 우선 적용 규칙을 통합해 개발/개발계 및 실운영의 쿠키 보안 레벨을 동적으로 정교화했습니다.
- **비즈니스 로직 격리 및 트랜잭션 롤백 보장**:
  - `src/app/services/leave_service.py`를 신설하여 다수일 신청, 일일 8시간 한도 및 잔여 연차 검증, 중복 시간대 검증을 캡슐화했습니다.
  - 연차 차감 로직의 원자적 동작을 위해 비즈니스 트랜잭션(`db.commit()` 및 예외 시 `db.rollback()`)을 서비스 레이어 내부로 격리하여 라우터의 책임을 간소화했습니다.
- **통합 검증 및 16개 핵심 시나리오 전원 통과**:
  - 리팩토링된 API 경로(`/api/`)를 테스트 시나리오 스크립트(`run_remaining_tests.py`)에 전면 반영하여 16개 시나리오 통합 검사를 완료하고 **전원 통과(All 16 Cases PASS)**를 획득했습니다.

---

## 📅 2026-05-23
### v1.5.10 연도별 연차 독립 격리화 및 관리자 배너 고도화
- **연도별 연차 관리 격리화 (Isolating Annual Leave)**:
  - 연차 개별 수정 및 일괄 지급 시 사원 프로필의 기본 한도 컬럼(`user.total_leave_hours`)을 덮어써서 다른 연도의 할당량까지 연쇄적으로 덮어씌워지던 데이터 간섭 버그를 해결하기 위해 해당 동기화 코드를 제거했습니다.
  - 이를 통해 각 연도별 연차 데이터가 정해진 연도 범위 내에서만 수정되고 저장되는 완전한 물리적 격리를 보장했습니다.
- **연말/연초 대시보드 권고 배너 및 자동 숨김 스마트 로직 도입**:
  - 매년 12월~1월 기간 중 관리자가 대시보드 진입 시 새해 연차 일괄 설정을 권고하는 Dense UI 알림 배너를 추가했습니다.
  - 단순 기간 기반 고정 노출의 번거로움을 덜기 위해, 데이터베이스에 내년도 연차 할당 정보(`UserYearlyLeaveAllocations` 레코드)가 최소 1건 이상 생성되면 대시보드 배너가 자동으로 사라지는 스마트 숨김 제어를 추가했습니다.
- **README 및 버전 정합성 검사 통과**:
  - `README.md` 내에 "5. 💡 연차 운영 가이드"를 구성하여 격리 정책 및 연말/연초 일괄 지급 프로세스를 명세했습니다.
  - 버전 동기화 `release.ps1 -Version 1.5.10 -BuildImage -RunChecks` 및 `build_portable.ps1` 검증 절차를 성공적으로 통과하여 포터블 바이너리를 갱신했습니다.

---

## 📅 2026-05-23
### v1.5.11 사원 강제 삭제 외래키 무결성 패치 및 연차 조회 속도 최적화
- **사원 강제 삭제(Hard Delete) 무결성 예외 해결**:
  - `api_admin.py`의 `hard_delete_user` API에서 사원 삭제 시 `AuditLogs` 테이블 내의 `actor_id` 컬럼 외래키 제약조건 위반으로 인해 `IntegrityError`가 발생하던 문제를 단일 벌크 쿼리로 `actor_id`를 `None`으로 일괄 업데이트하게 변경하여 예방했습니다.
  - 이를 통해 감사 로그 이력을 유실하지 않으면서도 안정적인 DB 참조 무결성을 보장하게 하였습니다.
- **연차 데이터베이스 쿼리 복합 인덱스 탑재**:
  - `models.py` 내의 `Leaves` 모델 테이블 스키마에 `status`와 `is_deductive` 컬럼에 대한 복합 인덱스(`ix_leaves_status_is_deductive`)를 추가 정의했습니다.
  - `migrations.py`에 자동 스키마 마이그레이션 DDL(`v1_5_11_leaves_status_deductive_index`)을 구성하여, 시작 시점에 복합 인덱스를 자동 생성하도록 아키텍처를 고도화했습니다. 이로써 대량 데이터 운용 시의 월별/연도별 연차 통계 및 관리자 타임라인 조회 속도를 대폭 최적화했습니다.
- **통합 검증 자동화 확장**:
  - `run_remaining_tests.py`에 `[CASE 17]` (사원 강제 삭제 시 감사 로그 정합성 검증) 테스트 케이스를 신설하여, API 성공 및 감사 로그 보존 및 actor_id NULL 전환 상태를 자동 검증 완료했습니다.

---

## 📅 2026-05-23
### v1.5.12 윈도우 포터블 백그라운드 프로세스용 시스템 트레이 아이콘(Systray) 통합 및 자체 아이콘 적용
- **네이티브 윈도우 시스템 트레이 아이콘 구현**:
  - 외부 라이브러리(Pillow, PyQt 등)에 의존하지 않고, Windows `ctypes` 표준 라이브러리만을 이용해 백그라운드 서버 프로세스(`SHIM_Portable.exe`)에 트레이 아이콘 기능을 통합했습니다.
  - 트레이 아이콘 우클릭 시 **"SHIM 브라우저 열기"** 및 **"종료"** 메뉴가 표출되도록 설계했습니다.
  - 마우스 더블클릭이나 브라우저 열기 클릭 시, 현재 실행 중인 포트로 브라우저가 자동 기동되도록 연결했습니다.
  - **"종료"** 선택 시 트레이 자원을 반환하며 프로세스를 안전하게 강제 종료(`os._exit(0)`) 처리했습니다.
- **TrackPopupMenu 클릭 외 영역 닫힘 버그 수정**:
  - 트레이 메뉴 활성화 시 바깥 영역을 클릭해도 닫히지 않는 Win32 팝업 메뉴 고유 결함을 `SetForegroundWindow` 호출 순서 보정과 `PostMessage(hwnd, WM_NULL, ...)` 활용으로 말끔히 수정했습니다.
- **SHIM 포터블 실행 파일 및 웹 브라우저 자체 아이콘(favicon) 적용**:
  - AI 이미지 생성을 활용해 SHIM 고유의 현대적인 favicon.ico를 생성하여 `src/static/favicon.ico`로 저장했습니다.
  - `build_portable.ps1` 스크립트를 보완하여 PyInstaller 빌드 시 `--icon` 옵션을 인자로 주어 실행 파일 아이콘을 삽입했습니다.
  - `base.html` 템플릿 헤더 영역에 favicon 링크 태그를 추가하여 브라우저에서 탭 아이콘이 정상 렌더링되도록 구현했습니다.

---

## 📅 2026-05-24
### P2 기능 고도화 및 구동/종료 안정성 개선
- **FastAPI Lifespan 도입 및 데이터베이스 안전 종료**:
  - `src/app/main.py`에 FastAPI 최신 표준인 `lifespan` 컨텍스트 매니저를 전면 적용했습니다.
  - 서버 종료 시점에 `database.engine.dispose()`를 명시적으로 호출하여, SQLite 데이터베이스 커넥션 풀 자원을 즉시 회수하고 파일 락(`.db-wal`, `.db-shm`)이 남지 않도록 Teardown 로직을 보장했습니다.
  - 기존 통합 테스트 코드가 startup_event를 개별 임포트하여 초기화하는 방식을 지원하기 위해, `main.py` 내에 호환용 동기식 `startup_event()` 함수를 복원하고 `lifespan` 내부에서 호출되도록 정렬했습니다.
- **다중 시트 통합 엑셀 다운로드 (Excel Export 고도화)**:
  - `/admin/leave/timeline/export` API를 리팩토링하여 2개 시트를 지닌 엑셀 통합본을 출력하도록 기능을 고도화했습니다.
    - **Sheet 1 ("연차 현황 요약")**: 전체 사원의 해당 연도 총 배정 연차 시간, 사용 완료(APPROVED) 시간, 대기(PENDING) 시간, 그리고 잔여 시간을 요약 출력.
    - **Sheet 2 ("상세 신청 내역")**: 차감 여부, 신청 사유 및 반려 사유가 포함된 세부 연차 타임라인 이력 출력.
  - **N+1 쿼리 최적화**: 엑셀 다운로드 시 SQLite I/O 부하를 예방하기 위해, 루프 내 개별 유저 쿼리를 배제하고 대상 사원의 연도 데이터를 `UserYearlyLeaveAllocations` 및 `Leaves` 테이블에서 2회의 Bulk 쿼리로 매핑 조회하여 파이썬 메모리상에서 병합 연산하도록 아키텍처를 고도화했습니다.
  - openpyxl 메모리 관리를 위해 `wb.close()` 처리를 원자적으로 보장했습니다.
- **포터블 런처 브라우저 자동 오픈 제거**:
  - `shim_portable.py` 런처 및 백그라운드 서버 기동 시 브라우저를 강제로 기동시키는 시도나 계획을 전면 제거하여 백그라운드 구동에 어울리도록 슬림화했습니다. (트레이 메뉴 우클릭 수동 오픈만 존치)
- **런처 다중 프로세스 Uvicorn 분리 및 Graceful Shutdown**:
  - Windows 환경의 `GenerateConsoleCtrlEvent` 및 `send_signal` 제약을 극복하기 위해, `--server` 기동 시 내부적으로 `subprocess.Popen`을 통해 자기 자신을 `--uvicorn-worker` 인자로 재귀 구동(Self-fork)하는 마스터-워커 프로세스 아키텍처를 적용했습니다.
  - 자식 Uvicorn 프로세스는 `subprocess.CREATE_NEW_PROCESS_GROUP` 플래그로 격리 기동하여 시그널 채널을 확보했습니다.
  - 트레이 아이콘 "종료" 수신 시, 윈도우 시스템 벌룬 알림으로 `"SHIM 서버가 안전하게 종료되었습니다."` 팝업을 전송하고 콘솔에 안내 메시지를 출력한 뒤 `time.sleep(1.5)` 대기 후 자식 프로세스에 `signal.CTRL_BREAK_EVENT` 시그널을 전달하여 Uvicorn과 FastAPI의 Graceful Shutdown을 완벽하게 수행했습니다.
- **통합 검증 결과**:
  - `run_remaining_tests.py`를 실행하여 17개 회귀 및 통합 테스트 시나리오가 모두 **PASS**함을 확인 완료했습니다.

---

## 📅 2026-05-24
### 테스트용 패키지(psutil, requests) 격리 및 자동화 테스트 가이드 정비
- **개발/배포 의존성 파일 물리적 분리**:
  - 메모리 누수 테스트 및 종료 테스트 목적의 외부 라이브러리(`psutil`, `requests`)가 배포판(Docker 이미지, 포터블 바이너리)에 원치 않게 포함되는 이슈를 차단하기 위해 `requirements.txt`에서 삭제하고 개발 전용 의존성인 `requirements-dev.txt`로 격리했습니다.
- **포터블 빌드(PyInstaller) excludes 추가**:
  - `SHIM_Portable.spec` 내 `excludes` 목록에 `'psutil'`, `'requests'`를 명시하여 단일 64-bit 실행 파일(`.exe`) 패키징 단계에서 개발 패키지가 실수로라도 탑재되는 현상을 완벽히 차단했습니다.
- **Graceful Shutdown & DB Teardown 자동화 테스트 스크립트 신설**:
  - 실제 구동 중인 Uvicorn 서버에 트래픽을 인위적으로 유도하는 도중 `CTRL_BREAK_EVENT` 시그널을 전달하고, 프로세스 종료 및 SQLite `.db-wal`/`.db-shm` 임시 파일의 완전한 머지/락 해제를 검사하는 통합 테스트 스크립트([test_graceful_shutdown.py](file:///W:/Documents/Project/SHIM/tools/scripts/test_graceful_shutdown.py))를 새로 제작해 등록했습니다.
  - 비대화형(Headless) 테스트 환경(네트워크 UNC 드라이브 속도 병목 포함)을 고려하여 소켓 `connect` 실시간 감지 및 타임아웃 튜닝(45초)을 적용해 완벽한 **PASSED** 판정을 획득했습니다.
- **인수인계 가이드 및 릴리즈 체크리스트 공식 명세화**:
  - [docs/3-2_AI_인수인계_가이드.md](file:///W:/Documents/Project/SHIM/docs/3-2_AI_인수인계_가이드.md)에 의존성 패키지 격리 정책과 성격별 테스트 케이스(정합성/비즈니스/안정성/메모리) 실행 명령어를 표 형태로 상세 수록했습니다.
  - [docs/1-3_운영_릴리즈_실행_체크리스트.md](file:///W:/Documents/Project/SHIM/docs/1-3_운영_릴리즈_실행_체크리스트.md)에 배포 빌드 전 필수 게이트웨이로 `test_graceful_shutdown.py`와 `test_memory_leak.py` 통과 여부를 체크박스로 등록하여 프로세스를 제도화했습니다.

### 성능 최적화 및 구동 방어 시스템 구축
- **Windows 런처 Mutex 기반 중복 실행 차단 구현**:
  - `shim_portable.py` 런처 기동 시 실행 파일 경로 MD5 해시 기반 전역 Mutex(`SHIM_Portable_Mutex_<Hash>`)를 선점하여 동일 경로 마스터 프로세스의 중복 기동을 물리적으로 차단했습니다.
  - 기존 마스터 윈도우(`SHIMTrayClass`)가 존재할 경우 `PostMessageW`를 통해 `ID_TRAY_TRIGGER_BALLOON` 이벤트를 날린 뒤 조용히 종료(`exit code 0`)되도록 구조를 설계했습니다. 수신받은 기존 트레이는 사용자에게 중복 기동 경고 벌룬 알림을 전송합니다.
  - 마스터가 self-fork하여 기동하는 자식 Uvicorn 프로세스(`--uvicorn-worker`)에 대해서는 뮤텍스 중복 체크를 우회(Pass)하도록 예외 예방 코드를 적용했습니다.
- **엑셀 다중 시트 다운로드 메모리 스트리밍 최적화 (WriteOnlyWorkbook)**:
  - `/admin/leave/timeline/export` API에서 수만 건의 엑셀 출력 시 메모리가 폭발하던 DOM 적재 문제를 극복하기 위해 `Workbook(write_only=True)` 스트리밍 모드를 도입했습니다.
  - 스트리밍 제약 조건에 부합하도록 첫 번째 시트("연차 현황 요약")와 두 번째 시트("상세 신청 내역")를 순차 생성해 행 단위로 데이터를 즉시 플러시하여 피크 메모리를 10MB 상수로 최적화했습니다.
- **통합 검증 및 단일 실행 자동화 테스트 추가**:
  - `run_remaining_tests.py` 시나리오 17종 전체 PASS를 통해 비즈니스 회귀 무결성을 증명했습니다.
  - 중복 기동 시나리오를 모의하는 자동화 테스트 스크립트([test_duplicate_execution.py](file:///W:/Documents/Project/SHIM/tools/scripts/test_duplicate_execution.py))를 새로 구축하여 런타임 검증을 무사히 통과(PASSED)시켰습니다.

### 배포 및 빌드 파이프라인 정합성 정밀 정비
- **개발/테스트 유산의 운영 이미지 누출 차단 (.dockerignore)**:
  - `.dockerignore` 파일에 `requirements-dev.txt`, `tools/scripts/test_*.py`, `tools/scripts/repro_*.py`, 임시 테스트 DB 디렉터리(`var/data_test*/`) 등을 명시하여 Docker 컨테이너 빌드 시 개발용 코드가 원격 이미지 내부에 불필요하게 복사되는 현상을 완벽히 차단했습니다.
- **버전 검증 및 릴리즈 파이프라인(verify_version_sync.ps1, release.ps1) 정합성 패치**:
  - FastAPI의 lifespan 컨텍스트 매니저 도입 과정에서 괄호 구조(`FastAPI(...)`)가 멀티라인 및 복수 인자 형태로 변경되어 기존 버전 싱크 검증/치환 스크립트가 오동작하던 이슈를 포착 및 긴급 패치했습니다.
  - 닫는 괄호를 제외한 버전 선언부 매칭 방식으로 정규식 및 단순 문자열 탐색 로직을 개선하여 향후 릴리즈 배포 파이프라인 구동 시 예외 없이 완벽하게 작동하도록 보장했습니다.
  - `.gitignore`에 AI 에이전트/툴 캐시 디렉터리(`.antigravitycli/`)를 추가해 로컬 환경 이물이 형상 관리에 유출되지 않도록 제어했습니다.

---

## 📅 2026-05-26
### 긴급 관리자 비밀번호 초기화 스크립트 외래키 결함 수정
- **오류 원인**: `emergency_reset_admin.py`에서 감사 로그(`AuditLogs`) 기록 시 `actor_id` 값을 `users` 테이블에 존재하지 않는 `"system"`으로 작성함에 따라 SQLite 외래키 제약조건(`PRAGMA foreign_keys=ON;` 활성화에 따른 `FOREIGN KEY constraint failed`) 위반 에러가 발생하던 현상을 해결했습니다.
- **수정 내용**: `actor_id` 값을 데이터베이스 상에 항상 존재하는 `"admin"` 계정 ID로 변경하여 외래키 정합성 조건을 통과하고 감사 로그가 안전하게 기록되도록 수정했습니다.
- **포터블 환경 비밀번호 초기화 기능 추가**: 기존 포터블 실행 환경(인터넷/Python 미설치 폐쇄망)에서는 `emergency_reset_admin.py` 파이썬 스크립트를 직접 가동할 수 없었던 문서-구조 정합성 결함을 발견하고 해결했습니다.
  - `shim_portable.py` 포터블 런처 자체에 `--reset-admin` CLI 옵션을 추가하고 내부에 비밀번호 초기화 로직을 이식하여, `SHIM_Portable.exe --reset-admin` 실행만으로 독립 PC에서도 관리자 암호를 즉시 안전하게 초기화할 수 있게 개선했습니다.
  - [1-2_백업_복구_유지보수_가이드.md](file:///w:/Documents/Project/SHIM/docs/1-2_백업_복구_유지보수_가이드.md) 내 포터블 환경에서의 비밀번호 초기화 지침을 최신화했습니다.

---

## 📅 2026-05-29
### 관리자 연차 타임라인 완료된 건 드롭다운 제거 및 UX 개선
- **요구사항**: 연차 신청 상태가 승인(APPROVED), 반려(REJECTED), 취소(CANCELED) 상태인 건은 어차피 다른 상태로의 전이가 불가능함에도 관리자 신청타임라인(`admin_leaves_timeline.html`)에서 변경 가능한 드롭다운이 보이는 어색함을 유발함.
- **개선 내용**:
  - 신청 상태가 `PENDING` (결재 대기)인 경우에만 상태 변경용 `<select>` 드롭다운을 노출하도록 분기 조건을 적용.
  - 이미 처리가 끝난 `APPROVED`, `REJECTED`, `CANCELED` 상태의 경우, 해당 상태에 매핑되는 고정된 Dense UI 스타일 텍스트 배지(Badge)를 출력하여 상태 변경이 완료되었음을 명확히 시각화.
  - 반려(`REJECTED`) 건에 대해 기존에 존재하던 반려 사유 텍스트 노출 로직은 훼손 없이 배지 하단에 정상적으로 표시되도록 정렬 유지.

### 동적 HSL 해시 기반 회사/팀 배지 구현 및 가시성/식별성 보정 (팀 우선순위 및 톤 보강)
- **요구사항**: 여러 회사와 팀이 텍스트로만 표시되던 영역을 색상 배지로 변환해 시각적 구분을 강화하되, 화면 내에서 사용자가 더 확실히 보아야 할 핵심 정보인 **'팀(Team)'** 배지가 시각적으로 두드러지도록(시각적 계층화) 조율함. 또한 연차 캘린더와 팀 캘린더 내에서도 동일한 배지 테마를 표시하되 조밀한 캘린더 가독성을 해치지 않도록 슬림하게 처리함.
- **개선 내용**:
  - `src/app/main.py`에 `string_to_hsl_style` 헬퍼 함수를 추가하고 Jinja2 전역 변수로 등록.
  - **시각적 우선순위 계층화 (Team-First Visual Hierarchy)**:
    - **팀 (1순위)**: 더 확실하고 또렷이 눈에 띄도록 생동감 있고 강렬한 색조 대역(`180~340도`), 높은 채도(`75%`), 낮은 배경 명도(`91%`), 어두운 텍스트(`15%`)를 적용해 확실한 가시성을 획득. 배지 형태도 둥근 캡슐형(`rounded-full px-2 py-0.5 text-[10px] font-semibold`)으로 차별화.
    - **회사 (2순위)**: 보조적인 정보이므로 눈에 덜 띄도록 차분한 색조 대역(`20~180도`), 낮은 채도(`52%`), 높은 배경 명도(`95%`), 부드러운 텍스트 명도(`32%`)를 적용해 은은하게 톤다운. 배지 형태도 각진 둥근 모서리(`rounded px-1.5 py-0.5 text-[10px] font-bold`)로 적용.
  - **해시 충돌 우회 (Salting)**: 해싱 전 회사명 앞에는 `"company:"`, 팀명 앞에는 `"team:"` 접두어(Salt)를 결합하여 해시 연산을 수행함으로써 회사와 팀의 고유 해시 씨앗(Seed)을 분리함. 이로써 `A-건설`과 `설계팀` 등의 우연한 색상 중복 현상을 완벽하게 방지함.
  - **가시성 보정**: 황색/라임색 등 가시성이 극히 취약한 구간(Hue 45~95도)은 텍스트 명도를 추가로 더 낮춰(팀: `10%`, 회사: `22%`) 시인성을 완벽히 만족하도록 보정.
  - **캘린더용 슬림 배지 설계 및 마크업 반영**:
    - 공간이 매우 협소한 캘린더 화면 특성을 반영하여 배지 패딩을 줄이고 텍스트 크기를 `text-[9px]`, 최대 가로 폭 제한(`max-w-full truncate`)을 설정하여 캘린더의 기존 레이아웃을 훼손하지 않는 슬림형 배지 적용.
    - 사원 관리([admin_users.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/admin_users.html)), 연차 타임라인([admin_leaves_timeline.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/admin_leaves_timeline.html)), PM 및 결재 관리([user_approvals.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/user_approvals.html)) 외에 어드민 연차 캘린더([admin_leaves_calendar.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/admin_leaves_calendar.html)) 및 유저 팀 캘린더([user_team_calendar.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/user_team_calendar.html))의 PM 전사 뷰 팀 목록에 배지 일괄 적용.
  - **캘린더 전용 가로 칸 확보 및 팀 컬럼 상시 노출 (추가 요구사항 대응)**:
    - **어드민 연차 캘린더 사원명 간소화 및 비활성 배지 제거를 통한 칸 확보**: [admin_leaves_calendar.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/admin_leaves_calendar.html) 의 사원명 컬럼에서 불필요한 사원 ID `(사원ID)` 텍스트를 제거한 데 이어, 추가적으로 비활성 사원에게 붙던 붉은색 '비활성' 배지 텍스트를 제거하고 반투명 스타일(opacity-40)로만 비활성 상태를 나타내도록 변경했습니다. 이를 통해 사원 칸 너비를 `4.25rem`으로 대폭 축소하여 추가 가로 칸을 확보했습니다. 또한 회사 및 팀 컬럼(`col-meta`) 너비를 `5.5rem`~`6.5rem`으로 넓히고 최대 너비 기준을 `7rem`으로 확장하여 가시성을 확보했습니다.
    - **팀 캘린더 전사 공유 시 팀 컬럼 상시 노출 및 너비 최적화**: [user_team_calendar.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/user_team_calendar.html) 내의 '팀' 컬럼 헤더 및 바디 셀을 PM 권한(`user_role == 'PM'`) 체크 바깥 영역으로 분리해 전사 공유 모드 기동 시 일반 사원 세션에서도 타 부서 인원의 소속 팀 배지가 상시 노출되도록 개선했습니다. 빈 데이터 렌더링 시 테이블이 무너지지 않도록 `colspan`을 `(4 if user_role == 'PM' else 2)`로 보정했습니다. 추가적으로 사원명 열 너비를 `4.25rem`으로 줄이고 패딩을 `!px-2`로 간소화하는 한편, 팀명 열 너비를 `7rem`으로 넓혀 "사립유치원회계" 등 최대 7자 팀명도 줄바꿈이나 잘림 없이 캘린더 테이블 내에 온전히 렌더링되도록 시각적 조율을 마쳤습니다.
    - **유저 사이드바의 동적 메뉴 라벨 변경**: [sidebar_user.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/partials/sidebar_user.html) 에서 전사 캘린더 공유 상태(`company_calendar_visible`이 True)일 때는 메뉴 라벨을 '프로젝트 캘린더'로 자동 변경해 노출하고, 소속 팀원 공유만 활성화된 경우에는 '팀 캘린더'로 노출되도록 동적 조건문 처리를 적용했습니다.
    - **팀 캘린더 전사 공유 설명문 텍스트 개선**: [user_team_calendar.html](file:///V:/M2SSD/Documents/Project/SHIM/src/templates/user_team_calendar.html) 에서 전사 공유 활성화(`company_calendar_visible`이 True) 상태 시 상단 제목이 '프로젝트 캘린더'로, 설명문이 '프로젝트의 연차 현황입니다'로 올바르게 출력되도록 템플릿 분기 조건을 수정하여 '프로젝트팀의'와 같은 불필요한 조사 합성 결함을 정정했습니다.
- **회귀 무결성 검증**:
  - `run_remaining_tests.py` 통합 회귀 테스트 스크립트를 재구동하여 UI 변경 및 헬퍼 추가로 인한 마크업 및 페이지 렌더링에 부정적 이펙트가 없음을 최종 확인 (총 17개 시나리오 전원 **PASS**).

### 데이터베이스 트랜잭션 안전성 강화 및 설정(브랜딩) 메모리 캐싱 도입 (v1.5.14 보강)
- **일부 CRUD API 내 데이터베이스 트랜잭션 롤백 보강**:
  - `api_admin.py`의 `hard_delete_user`(사원 강제 삭제 API), 시스템 설정 업데이트 API 4종 및 `api_user.py`의 `change_password`(사용자 암호 변경 API) 등에 명시적인 `try-except SQLAlchemyError` 예외 차단막을 적용했습니다.
  - 이를 통해 데이터 저장/변경 오류 시 강제 롤백(`db.rollback()`) 처리를 연동하고 화면 단에 500 예쁜 에러 안내 JSONResponse를 반환하도록 리팩토링했습니다.
- **브랜드 및 시스템 설정 미들웨어 성능 최적화 (인메모리 캐싱)**:
  - 매 HTTP 요청마다 동기식 DB 커넥션을 열고 닫으며 `SystemSettings`를 조회하던 `branding_middleware` 병목 문제를 극복하기 위해 `leave_policy.py`에 인메모리 캐시 전역 변수(`_SYSTEM_SETTINGS_CACHE`)를 설계했습니다.
  - `get_system_settings(db)` 호출 시 기본적으로 메모리 내 캐시 값을 우선 반환하도록 최적화하였으며, 어드민 설정 변경 API에서 값이 저장될 때만 `force_reload=True` 분기로 캐시를 무효화 및 리프레시하도록 구현했습니다. 이로써 일반 사용자 화면 이동 시 발생하는 쓸데없는 DB 커넥션 오버헤드를 0(Zero)으로 최적화했습니다.
- **회귀 테스트 통과**:
  - `run_remaining_tests.py`를 통해 모든 비즈니스 회귀 테스트 17종 전원 **PASS**를 획득하여 무결성을 재입증했습니다.

### 데이터베이스 기동 및 종료 시점의 임시 바구니 비우기(체크포인트) 최적화 (v1.5.14 추가 보강)
- **임시 파일 찌꺼기 방지 및 자동 동기화 (대칭식 자동 복구 안전망)**:
  - 포터블 및 도커 실행 환경의 구동 안정성을 극대화하기 위해, [main.py](file:///v:/M2SSD/Documents/Project/SHIM/src/app/main.py)의 `startup_event()`와 `lifespan` 컨텍스트 종료 시점에 명시적으로 `PRAGMA wal_checkpoint(TRUNCATE);` 구문을 실행하도록 보강했습니다.
  - **대칭 복구 안전망 설계**: 만약 예기치 못한 비정상 종료(갑작스러운 PC 전원 차단 등)로 인해 종료 시점에 파일 병합이 수행되지 못해 `.db-wal` 찌꺼기 파일이 남아있더라도, **다음번 프로그램 재기동(부팅) 시점에 가장 먼저 임시 바구니 데이터를 본 DB로 유실 없이 안전하게 복구/병합한 뒤 리셋(Truncate)**하도록 이중 설계했습니다.
  - 정상 종료 시에는 윈도우 탐색기 상에 지저분한 임시 저널 파일(`.db-wal`, `.db-shm`)이 남지 않도록 완벽히 자동 정리하여 복사 및 오프라인 배포 관리 편의성을 제고했습니다.
- **통합 검증 완료**:
  - `run_remaining_tests.py` 통합 회귀 검증 17종 테스트를 다시 가동하여 정상적으로 전원 **PASS**됨을 검증했습니다.

### Python 3.12 권장 가이드라인 준수를 위한 날짜 함수 리팩토링 (v1.5.14 보강)
- **datetime.utcnow() 제거 및 timezone.utc 반영**:
  - Python 3.12 런타임 환경에서 발생하는 경고(deprecation warning)를 예방하기 위해 [auth.py](file:///v:/M2SSD/Documents/Project/SHIM/src/app/auth.py)의 JWT 토큰 만료 시간 생성 코드 내 `datetime.utcnow()` 호출을 제거하고, 현대 표준인 `datetime.now(timezone.utc)` 방식으로 리팩토링했습니다.

### 데이터베이스 자가 진단, 감사 로그 인덱스 최적화 및 점심시간 중첩 차단 (v1.5.15 추가)
- **데이터베이스 자가 정합성 무결성 진단 (Startup Self-Diagnostic)**:
  - 비정상 기동이나 강제 종료로 인한 SQLite DB 물리 파일 깨짐을 미연에 감지하도록 `main.py`의 `startup_event()` 첫머리에 `PRAGMA quick_check;`를 통한 자가 진단 로직을 도입했습니다. 손상 감지 시 콘솔 경고 로그와 함께 핫 백업 복구 절차를 상세히 안내합니다.
- **AuditLogs 조회 성능 가속화를 위한 스키마 인덱스 추가**:
  - 300인 5년 장기 적재 시 `audit_logs` 테이블의 Table Full Scan을 방지하기 위해 `actor_id`, `timestamp`, `action` 필드에 인덱스(`Index`)를 정의했습니다. 아직 정식 출시 전 개발 단계이므로, 지저분한 임시 마이그레이션 이력(`migrations.py`)은 생성하지 않고 데이터베이스 설계 원본인 `models.py`에 직접 인덱스를 명시하여 신규 데이터베이스 설치 시 단번에 깔끔하게 인덱싱이 적용되도록 조치했습니다.
- **연차 신청 시 점심시간 경계 입력 제어 및 실시간 1차 차단**:
  - 사용자가 시간 드롭다운으로 연차를 신청할 때, 선택 구간이 점심시간 범위 내에 완전히 속해 실질 차감 시간이 0시간이 될 경우 백엔드(`leave_policy.py`)에서 구체적이고 상세한 예외 메시지를 반환합니다.
  - 프론트엔드(`user_dashboard.html`)에서도 드롭다운 값이 바뀔 때마다(`onchange`) 실시간 유효성 함수(`onTimeSelectionChange()`)를 호출하여 점심시간 겹침 감지 시 등록 버튼을 비활성화하고 폼 하단에 즉각 경고 라벨을 표시하도록 1차 안전 차단벽을 구축했습니다.
- **테스트 케이스 보강 및 회귀 무결성 검증**:
  - `run_remaining_tests.py` 통합 회귀 시나리오 `[CASE 14]`에 점심시간 완전 중첩(12:00~13:00) 신청에 대한 400 Bad Request 및 구체적 안내 텍스트 검증 단계를 추가하여 17종 전체 케이스 전원 **PASS**를 달성했습니다.

### Excel 타입 정밀화 및 사원 세션 즉시 무효화 (v1.5.16 추가)
- **Excel 다중 시트 다운로드 시의 타입 포맷팅 정밀화**:
  - 관리자가 연차 대시보드 타임라인을 엑셀로 다운로드할 때 연차 시간 등의 데이터가 단순 문자열이 아닌 정교한 숫자(Number)와 날짜(Date) 타입으로 포맷팅되도록 보강했습니다.
  - "총 배정 시간", "사용 완료 시간", "결재 대기 시간", "잔여 시간" 및 상세 신청 내역의 "차감시간" 수치 데이터를 `float`/`int` 타입으로 명확히 주입하여 엑셀 자체의 수식 연산(SUM 등)이 가능하도록 보완했습니다.
  - 상세 신청 내역의 "신청 시각" 및 "연차일" 데이터를 `datetime`/`date` 객체로 입력하고, openpyxl of `WriteOnlyCell`를 활용하여 각각 `yyyy-mm-dd hh:mm:ss`, `yyyy-mm-dd` 서식을 명시적으로 지정함으로써 엑셀 내에서 날짜 타입으로 온전히 정렬, 필터링 및 시간 간 계산이 가능하도록 개선했습니다.
  - Stateless JWT 토큰 인증 구조의 취약점을 보완하기 위해, `models.py` 내 `Users` 테이블 스키마에 직접 `token_version` 컬럼을 명시하여 신규 설치 및 테스트 빌드 시 단번에 일괄 반영되도록 설계했습니다. (아직 정식 배포 전 개발 단계이므로 불필요한 임시 마이그레이션 DDL 스크립트는 누적하지 않고 생략했습니다.)
  - 관리자가 사원의 정보를 수정(권한 변경, 비활성화)하거나 패스워드를 초기화할 때, 그리고 사용자가 본인의 패스워드를 직접 변경할 때 대상 사원의 `token_version`을 1씩 증가시키도록 비즈니스 로직을 보강했습니다.
  - 로그인 성공 시 발급하는 JWT 토큰 payload에 발급 시점의 `token_version`을 포함시켰으며, 요청 검증 미들웨어(`dependencies.py`)에서 DB의 최신 `token_version`과 대조하여 불일치(혹은 누락)할 시 `NotAuthenticatedException`을 발생시켜 기존 구버전 토큰 세션을 실시간으로 강제 만료 처리하도록 고도화했습니다. (하위 호환성과 기존 테스트 환경 통과를 위해 미지정 토큰은 버전 0으로 취급하여 대조합니다.)
- **테스트 케이스 보강 및 회귀 무결성 검증**:
  - `run_remaining_tests.py` 통합 회귀 시나리오에 사원의 권한/상태 토글 시 구 세션이 즉각 만료되는 무효화 테스트 시나리오(`[CASE 10]`) 및 엑셀 다운로드 파일 내의 수치/날짜 셀 타입 및 Number Format 무결성을 체크하는 시나리오(`[CASE 11]`)를 신설하여 전체 19개 시나리오 테스트를 성공적으로 통과(PASS)시켰습니다.

---

## 📅 2026-05-29
### 사원 관리 화면 정렬 기준 고도화 및 비활성 사원 최하단 고정 (v1.5.17 추가)
- **사원 관리 정렬 로직 및 기본 정렬 추가**:
  - 사원 관리(`/admin/users`) 진입 시의 기본 정렬(`sort_key` 디폴트)을 `"role"`로 변경했습니다.
  - 권한 우선순위 기준은 시스템 관리자(`ADMIN`) ➔ PM ➔ 팀장(`TEAM_LEAD`) ➔ 일반 사원(`STAFF`) 순서로 적용되며, 동일 권한 내에서는 2차 정렬 조건으로 이름(`user_name`) 오름차순이 되도록 튜플 키(`_user_sort_tuple`)를 구성했습니다.
  - 사원 목록의 "권한" 테이블 헤더를 정렬 가능한 링크(`sort_urls.role`)로 전환하여 사용자가 클릭했을 때 권한별 정렬이 정순/역순으로 동작하도록 설계했습니다.
- **비활성 사원의 최하단 고정 격리**:
  - 관리자가 어떤 정렬 컬럼이나 오름차순/내림차순 정렬 방향을 클릭하더라도 비활성 상태(`is_active = False`) 사원은 항상 테이블 맨 아래에 격리되어 위치하도록 활성 사원 그룹(`active_users`)과 비활성 사원 그룹(`inactive_users`)을 각각 정렬한 후 합치는 병합 정렬 모델을 적용했습니다.
- **통합 검증 및 회귀 무결성 검사**:
  - `run_remaining_tests.py` 통합 회귀 시나리오 `[CASE 12]`를 신규 개설하여 기본 정렬(권한별 + 이름 오름차순), 역순 정렬(권한 역순 + 이름 내림차순), 이름 정렬 시 비활성 사원 최하단 고정 및 활성 사원 정렬 결과를 완벽하게 검증(PASS)했습니다.

## 📅 2026-05-30
### 메모리 누수 검증 기준 보정 및 비상 비밀번호 초기화 세션 만료 동기화
- **메모리 누수 검증 스크립트 수정**:
  - `tools/scripts/test_memory_leak.py`에서 물리 메모리(RSS) 증가량 한도(10MB)가 openpyxl의 메모리 풀링 및 C 레벨 버퍼 점유로 인해 매번 초과되어 경고를 유발하는 현상을 해결하기 위해, `tracemalloc` 기준 파이썬 실제 힙 누출량(1.0MB 미만)을 주 통과 요건으로 전환하고 RSS 한도는 30.0MB로 상향하여 비정상 폭증만 보조 검증하도록 개선했습니다.
- **긴급 관리자 초기화 시 세션 무효화 정합성 패치**:
  - v1.5.16에서 도입된 토큰 기반 구 세션 무효화(`token_version` 비교) 흐름이 긴급/비상 관리자 패스워드 리셋(CLI 및 스크립트) 경로에서는 누락되어 있던 사안을 확인했습니다.
  - `portable/shim_portable.py` 및 `tools/scripts/emergency_reset_admin.py` 내부의 admin 초기화 로직에 `admin.token_version += 1`을 명시적으로 추가하여 비밀번호 초기화 즉시 기존 활성화되어 있던 관리자의 브라우저 쿠키/세션이 실시간 강제 만료되도록 정합성을 일치시켰습니다.
- **회귀 무결성 검증**:
  - 전체 회귀 테스트를 재실행하여 수정한 초기화 로직 및 세션 검증이 문제없이 작동함을 최종 확인했습니다.
- **추가 시스템 안정성 및 보안성 개선**:
  - **어드민 Write API 트랜잭션 롤백 보강**: `api_admin.py` 내부의 공휴일 추가/수정/삭제, 개인 연차 삭제, 사용자 개별/일괄 연차 일수 변경 및 연차 차감/비차감 유형 수정 등 8개 Write API에서 `try-except SQLAlchemyError` 예외 차단막을 적용하여 DB 동시성 제어 오류(SQLite 락 등) 시 강제 롤백 처리하고 사용자에게 안전한 500 에러 JSON 응답을 반환하도록 개선했습니다.
  - **기본 비밀번호(0000) 권장 경고 배너 구현**:
    - 사용자가 기본 비밀번호인 `0000`으로 로그인 시, JWT 페이로드에 `is_default_password` 플래그를 심어 전달하도록 `/login` API를 수정했습니다.
    - `get_current_user` 의존성을 거쳐 `request.state`에 이를 저장하고, Jinja2 글로벌 템플릿 컨텍스트 프로세서(`branding_template_context`)에 연동하여 무버헤드(No DB hit on every page load)로 템플릿 전역에서 사용하도록 흐름을 잡았습니다.
    - [base.html](file:///W:/Documents/Project/SHIM/src/templates/base.html) 레이아웃 상에 현재 기본 비밀번호(0000) 사용 중일 시 "비밀번호 변경" 모달 열기 버튼을 포함한 눈에 띄는 앰버 색상 경고 배너를 출력하여 실무 운영 환경의 보안 안전성을 극대화했습니다.
  - **데이터베이스 예외 발생 시 상세 오류 안내 메시지 구현**:
    - `api_admin.py`와 `api_user.py` 전역의 `except SQLAlchemyError as e:` 예외 차단막 블록에 적용하여 데이터베이스 롤백 시 현업 관리자에게 상황을 직관적으로 알리는 JSON 응답을 전송하도록 고도화했습니다.
- **마스터 데이터 관리 화면 설명 구문 보완 및 안내 계층 구조화**:
  - [admin_master.html](file:///W:/Documents/Project/SHIM/src/templates/admin_master.html)의 백업 경로 정보 교정(`var/data/backup/`), PM 자동 승인/팀장 자가 승인 불가 규칙 보강, 점심시간 중첩 및 최소 단위 체크 제약 추가.
  - 하단 '운영/상태 안내' 영역의 폰트 크기 세분화(13px/12px/11px) 및 상태값(결재 대기, 승인됨, 반려, 신청 취소)에 Dense UI 피드백 컬러 코드를 적용하여 시각적 구분을 대폭 개선했습니다.
- **연차 캘린더 화면 범례 동기화 및 종합 안내 팁 박스(Info Box) 도입**:
  - [admin_leaves_calendar.html](file:///W:/Documents/Project/SHIM/src/templates/admin_leaves_calendar.html) 및 [user_team_calendar.html](file:///W:/Documents/Project/SHIM/src/templates/user_team_calendar.html)의 범례 칩 스타일(그라데이션 및 테두리)을 캘린더 내부 셀 렌더링 스타일과 동기화했습니다.
  - 실제 그리드 셀과 일치하도록 대기 중인 연차 범례 칩에서 불필요한 점선 스타일을 완전히 제거하고, 순수 반투명(`bg-dense-blue/55 filter grayscale-[35%]`) 칩과 텍스트(`대기 중인 연차 (반투명)`)로 일치시켰습니다.
  - 연간 보기 안내문을 가독성 높은 팁 박스 구조(`bg-dense-surface-soft p-3 rounded-xl`)로 고도화하여 계층 수준을 맞췄습니다.
- **전사 비동기 모달 및 입력 폼 엔터 전송(Enter Submit) 키보드 접근성 패치**:
  - [base.html](file:///W:/Documents/Project/SHIM/src/templates/base.html) (비밀번호 변경 모달 2개), [admin_users.html](file:///W:/Documents/Project/SHIM/src/templates/admin_users.html) (사원 일괄/추가/수정 모달 3개), [user_dashboard.html](file:///W:/Documents/Project/SHIM/src/templates/user_dashboard.html) (직접신청/캘린더신청 폼 2개) 총 7개 비동기 폼에 대해, 변경하기 버튼이 Form 바깥 영역에 존재해 키보드 엔터 제출이 불가하던 현상을 해결했습니다.
  - 각 form 태그에 `onsubmit="...(); return false;"` 리스너를 결합하고 버튼을 `type="submit"` 및 `form="FormID"`로 변경 연동하여 네이티브 키보드 엔터 제출 접근성을 완벽하게 보완했습니다.
- **포터블 실행 바이너리 Mutex 교착 회복 구조 개선**:
  - 비정상 종료 시 잔존 뮤텍스 커널 객체로 인해 재기동이 막히는 윈도우 포터블 기동 교착 상태를 정상 복구/스캔하는 구조를 이식하여 포터블 무설정 실행의 장기 종합 기동 안정성을 극대화했습니다.

---

## 📅 2026-05-30
### v1.5.18 포터블 런처 Mutex 기동 충돌 및 권한 문제 개선
- **윈도우 포터블 Mutex 안정성 및 버그 조치**:
  - **Mutex 네임스페이스 변경 (`Global\` ➔ `Local\`)**: 일반 권한 실행 환경에서 `Global\` 네임스페이스 뮤텍스 생성 시 발생하던 `Access Denied (WinError 5)` 오류로 인해 중복 실행 제어가 무력화되던 보안 취약점/오작동을 해결하고자 세션 로컬 네임스페이스인 `Local\`로 격리 조정했습니다.
  - **자식 서버 기동 충돌 해소**: 런처가 Mutex를 선점한 상태에서 백그라운드 서버 프로세스(`--server`)를 띄울 때, 동일 경로상의 Mutex가 이미 존재하므로 자식 프로세스까지 중복 기동 감지 분기에 걸려 즉시 죽어버리던 치명적 기동 버그를 발견하여 해결했습니다.
  - **Mutex 소유권 이식 구현**: 런처가 백그라운드 서버 프로세스를 `subprocess.Popen`으로 띄우기 직전에 `release_mutex` (CloseHandle API 호출)를 수행하여 소유권을 반환하도록 개선했습니다. 이를 통해 스폰된 서버 프로세스가 정상적으로 Mutex를 생성 및 점유하고 런처 창은 안전하게 종료될 수 있는 구조적 대칭성과 중복 제어를 실현했습니다.
- **통합 검증 및 릴리즈 동기화**:
  - `test_duplicate_execution.py` 중복 실행 방지 전용 테스트 및 `run_remaining_tests.py` 통합 회귀 시나리오 17종 전원 통과(**PASS**)를 확인했습니다.
  - `release.ps1`을 가동하여 `package.json`, `main.py`, `base.html`, `docker-compose.yml` 등 전체 버전 명세를 `1.5.18`로 일관되게 상향 동기화 및 패키징 검증을 마쳤습니다.

### v1.5.19 포터블 자식 프로세스 셧다운 강제 종료 안전망 (Force Kill Fallback) 추가
- **자식 프로세스 종료 보장 로직 개선**:
  - `shim_portable.py`의 `graceful_exit` 및 KeyboardInterrupt 핸들러 블록에서 `CTRL_BREAK_EVENT` 전송 후 `wait(timeout=3.0)` 동안 대기하게 하고, 만약 Uvicorn 자식 프로세스가 타임아웃 이내에 정상 종료하지 못할 시 `_uvicorn_proc.kill()`을 직접 호출하도록 복구 코드를 적용했습니다.
  - 이를 통해 Uvicorn 프로세스가 고립되어 시스템 종료 후에도 포트가 유실/점유된 채로 남게 되는 결함을 완벽히 원천 차단했습니다.
- **통합 검증 및 버전 동기화**:
  - `test_graceful_shutdown.py` 셧다운 자동 테스트 및 `run_remaining_tests.py` 통합 회귀 시나리오 17종 테스트를 다시 작동하여 정상 **PASS**를 획득했습니다.
  - `release.ps1`을 실행하여 전체 버전 명세를 `1.5.19`로 동기화 완료했습니다.

---

## 📅 2026-05-31
### v1.6.0 감사 로그 수행자 스냅샷 기록, PII 개인정보 선택적 암호화 및 초경량 자가 복구 핫 백업 기능 구현
- **감사 로그(AuditLogs) 수행자 스냅샷 컬럼 추가**:
  - `models.py` 내 `AuditLogs` 모델에 `actor_name` (String) 및 `actor_department` (String) 컬럼을 추가하고, SQLAlchemy `before_insert` 이벤트 리스너를 결합하여 인서트 시점의 수행자 사원 프로필(이름 및 소속 회사/팀)을 스냅샷으로 복사하여 영구 보존하도록 처리했습니다.
  - `admin_audit.html` 템플릿과 `api_admin.py` 엑셀 내보내기 로직을 수정하여 사원 Hard Delete 시에도 `(삭제됨)` 명시와 과거 스냅샷 정보(이름, 소속)를 안정적으로 노출되도록 개선했습니다.
- **PII 개인정보 컬럼 선택적 암호화 및 복구 이식성 보장**:
  - `cryptography` 라이브러리의 Fernet 대칭 암호화 기능을 이용해 SQLAlchemy 커스텀 `TypeDecorator`인 `EncryptedString`을 구현하고, `Users.user_name`, `Leaves.reason`, `Leaves.rejection_reason`에 바인딩했습니다.
  - 제공된 `SHIM_SECRET_KEY`를 SHA-256 해시하여 32바이트 대칭 키를 유도하여 동작하도록 하였으며, 키 미제공 또는 자동 생성된 `secret.key`일 시에는 투명하게 평문으로 동작하도록 폴백(Fallback)을 설계해 포터블/제로 컨피그 이식성을 준수했습니다. 기존 평문 데이터 조회 시에도 자동으로 폴백 복호화하여 투명한 하위 호환성을 제공합니다.
  - `1-2_백업_복구_유지보수_가이드.md` 문서에 키 이전 필요성 지침을 추가했습니다.
- **초경량 백그라운드 핫 백업 및 자가 복구 (Fail-safe)**:
  - SQLite 네이티브 API의 `.backup()` 메소드를 활용한 핫 백업 모듈 `src/app/services/ops.py`를 신설하고 최대 5개 백업본 순환 정책(Rotation)을 설계했습니다.
  - FastAPI lifespan 내 1일 1회 정기 백업 스케줄러와 데이터 수정 API 완료 시 `BackgroundTasks`를 활용한 실시간 변경 트리거 핫 백업을 연동했습니다.
  - 서버 시작 시 `PRAGMA quick_check;`를 실행하여 물리 손상 감지 시 손상 본을 `_corrupted_<timestamp>`로 격리 보관하고 가장 최근의 정상 백업본(`.bak`)을 자동 복원하는 자가 복구(Self-recovery) 기동 메커니즘을 완성했습니다.
- **데이터베이스 릴리즈 베이스라인 Squash 통합**:
  - 정식 1.6.0 배포 직전이므로, 개발 과정에서 누적되었던 기존 임시 DDL 마이그레이션들을 SQLAlchemy 원본 스키마와 통합(Squash)하여 제거했습니다.
  - `src/app/migrations.py` 내의 DDL 컬럼 추가 함수들을 소거하고 기본 테이블 빌드로 직통하게 하여, 신규 DB 구축 시 깔끔한 단일 베이스라인 스키마 생성을 보장했습니다.
- **포터블 초기 기동 시 `secret.key` 사용자 대화형 설정 지원**:
  - `portable/shim_portable.py` 실행 시 `secret.key`가 없을 경우 사용자에게 비밀키 값을 콘솔로 입력받는 초기 설정 기능을 구현했습니다.
  - 사용자가 키값을 입력하면 해당 키가 `secret.key`에 저장되어 암호화가 활성화되고(소켓 등 다용도 복제 이식성 제공), 미입력 시에는 자동 무작위 키 생성 및 평문 폴백으로 작동합니다.
- **백업 스레드 락 및 CancelledError 좀비 방지 강화**:
  - `daily_backup_scheduler` 루프에서 `asyncio.CancelledError` 발생 시 로깅 후 re-raise 처리하여, 서버 종료 시 백그라운드 태스크가 취소 상태를 삼키지 않고 깔끔하게 소멸되도록 좀비 방어를 적용했습니다.
  - 백업 처리 함수인 `run_backup_and_rotate`에 `threading.Lock`을 결합해 동시 다발적인 백업 쓰기 및 SQLite DB 락 경합을 방지하고, 파일명에 마이크로초(`%f`)를 덧붙여 파일 쓰기 충돌을 원천 차단했습니다.
- **통합 검증 및 릴리즈 동기화**:
  - `run_remaining_tests.py` 통합 회귀 시나리오 17종 전원 통과(**PASS**)를 확인했습니다.
  - `release.ps1`을 가동하여 전 버전을 `1.6.0`으로 동상향 동기화 및 패키징 빌드 테스트를 마쳤습니다.

---

## 📅 2026-06-01
### v1.6.0 비밀키 일관성 검증을 통한 데이터 오염 원천 차단 (Fail-Fast) 구현
- **비밀키 일관성 검증 엔진 (Fail-safe Key Verification)**:
  - 기동 시점(`startup_event()`)에 현재 실행 환경에 인가된 비밀키의 SHA-256 해시값과 데이터베이스 `SystemSettings.key_hash_snapshot`에 기록된 값을 대조하여, 불일치 시 기동을 즉시 차단(Fail-Fast)하고 프로세스를 즉각 안전하게 종료(`sys.exit(1)`)하는 보호 메커니즘을 적용했습니다.
  - 이를 통해 로컬 환경의 `.env` 파일 유실이나 포터블 환경의 입력 실수로 인해 DB 내 PII 데이터가 오염되고 검색 기능이 오작동하는 인시던트를 원천 방어합니다.
- **과도기/평문 데이터베이스 자가 판별 (Auto-Detection)**:
  - 패치 직후 최초 기동이나 신규 데이터베이스 설치 시와 같이 스냅샷 컬럼이 `NULL`인 상황을 처리하기 위해, DB의 실제 데이터 형식을 감사(gAAAAAB Fernet 암호문 접두사 분석)하여 평문/암호화 운영 여부를 자동으로 판별합니다.
  - 평문 DB에 키를 주입하거나 암호화 DB에 키를 유실한 모순 기동 조건을 완벽히 차단하며, 안전한 기동 조건일 때만 현재 키 해시(평문은 `"PLAINTEXT_MODE"`)를 스냅샷으로 고정 기록합니다.
- **가이드 문서 보강 및 무결성 검증**:
  - `1-2_백업_복구_유지보수_가이드.md` 및 `4-1_SHIM_프로젝트_설계서.md`에 보안키 검증 구조 및 기동 오류 조치 프로세스 가이드를 통합 반영했습니다.
  - `run_remaining_tests.py` 내에 `[CASE 13] Secret Key Consistency & Fail-Fast Verification` 시나리오를 신설하여 평문/암호화 불일치 및 키 불일치 기동 제어 로직이 윈도우 파일 락 에러 없이 완벽히 동작하는지 검증을 완료했습니다.

---

## 📅 2026-06-01
### v1.6.1 한 달 기준 그리드 캘린더 뷰 추가 및 탐색 사용성 개선
- **개인 대시보드 캘린더 1개 월 스위처 전환**:
  - 기존 12개월 전체를 격자형으로 나열하던 연차 캘린더를 1개 월 단위의 대형 그리드로 전환했습니다.
  - 이전 달(`<`) 및 다음 달(`>`) 이동 화살표를 배치하고, 페이지 리로드 없이 JavaScript를 사용하여 즉각적으로 활성화된 월별 그리드를 보이게/숨기게 처리했습니다.
  - 기동 시 사용자의 현재 날짜 기준 오늘이 속한 달을 자동으로 감지하여 기본 화면으로 활성화합니다.
  - 날짜 셀 높이를 확대(`h-20`)하고 내부 정렬을 개선하여, 휴가 유형과 공휴일 배지 및 시간 소모 표기가 겹침 없이 큰 배지로 깔끔하게 표시되도록 가독성을 극대화했습니다.
- **전사(팀) 및 관리자 캘린더 탭 전환기 및 그리드 캘린더 뷰 구현**:
  - 기존의 세밀한 "타임라인 뷰(Gantt/스프레드시트 행렬)"를 완벽하게 보존하면서, 동일 월을 달력 형태로 볼 수 있는 **"캘린더 뷰"** 기능을 신규 개발 및 결합했습니다.
  - 화면 상단에 탭 바(`타임라인 뷰` ↔ `캘린더 뷰` / 관리자는 `타임라인형` ↔ `달력형` ↔ `연간 요약`)를 추가하여 사용자가 원하는 형태로 휴가 현황을 볼 수 있게 하였습니다.
  - 달력형 그리드 뷰 상의 각 일자 셀 내에 해당 날짜에 부재중인 임직원들의 일정을 **색상별 콤팩트 이벤트 칩(Approved: 연차-적색, 반차-황색, 공가-보라색, 기타-청색 / Pending: grayscale 0.55 opacity)** 형태로 시각화했습니다.
  - 셀 내 부재 인원이 많을 시 레이아웃 깨짐을 방지하기 위해 칩 컨테이너 영역에 고정 높이와 개별 세로 스크롤바(`overflow-y-auto`)를 부여했습니다.
  - 관리자 캘린더의 일자별 칩은 클릭 시 기존 팝업 세부 시간대 조회 모달(`showLeaveTimeDetails`)과 정상 바인딩되도록 동작 정합성을 완벽하게 유지했습니다.
- **필터 및 정렬 상태 보존 기반 월 이동 화살표 탑재**:
  - 팀 및 관리자 캘린더 타이틀 영역 좌우에 이전/다음 달 이동 버튼을 탑재하여 1클릭 간편 달 이동이 가능하도록 구현했습니다.
  - 달 이동 버튼 클릭 시, 현재 쿼리 파라미터로 선택되어 있는 정렬 상태와 검색 필터 조건(회사, 부서, 사원 ID, 계정 활성 상태)들이 유실되지 않고 안전하게 전달(Jinja2 쿼리 스트링 결합)되도록 URL 설계 정책을 수립했습니다.
- **무결성 검증 및 회귀 테스트**:
  - `run_remaining_tests.py` 통합 회귀 시나리오 17종 테스트를 작동하여 정상 **PASS**를 확인했습니다.

---

## 📅 2026-06-03
### 마이그레이션 스쿼시 잔재 청소 및 테스트 데이터 시딩 개선
- **마이그레이션 스쿼시 찌꺼기 파일 삭제**:
  - v1.6.0 베이스라인 스쿼시 완료에 따라 사용 가치가 없어진 이전 과도기 임시 DDL 스크립트 `tools/scripts/migrate_v1_5_0.py` 파일을 리포지토리에서 안전하게 삭제(`git rm`)하여 프로젝트 청결성을 확보했습니다.
- **테스트 데이터 시딩 스크립트(`seed_test_data.py`) 최신 DDL 구조 및 비즈니스 정합성 호환성 개선**:
  - **1) 안전한 DB 초기화 옵션 `--reset` (`-r`) 구현**: 기존 DB 파일(`shim_internal.db`) 및 WAL 관련 저널 파일(`.db-wal`, `.db-shm`)이 런타임에 락을 형성하여 기동 실패를 야기하지 않도록, 세션 리소스를 명시적으로 정리(dispose)한 뒤 OS 수준에서 파일을 완전히 삭제하고 최신 `models.py` 정의를 기준으로 스키마를 재구성하여 시딩하도록 초기화 파라미터를 추가했습니다.
  - **2) 연차 생성 대상 연도의 연차 할당량(`UserYearlyLeaveAllocations`) 보장**: 연도 경계 기간(예: 연초 등)에 시드를 실행하여 과거/미래 연차가 이전 연도 등에 걸치는 경우 할당량 부재로 계산 오류가 발생하지 않도록, 생성되는 모든 휴가 날짜의 연도를 동적으로 수집하여 연도별로 `UserYearlyLeaveAllocations` 레코드가 사용자마다 안전하게 자동 생성되도록 개선했습니다.
  - **3) 연차 사용량의 연간 한도(120시간) 내 생성 제한**: 무작위 휴가 생성으로 인해 차감성 연차(`is_deductive=True`, APPROVED/PENDING 상태) 시간의 합이 연간 기본 한도(120시간, 15일)를 초과해 잔여 연차가 음수(마이너스)로 뒤틀리는 현상을 차단했습니다. 사용자별/연도별 누적 차감 시간을 동적으로 추적 및 계산하여 한도 내에서만 연차를 신청 및 생성하도록 안전 제약을 강화했습니다.
  - **4) 감사 로그 액터 스냅샷(`actor_name`, `actor_department`) 누락 방지**: `AuditLogs` 데이터 시딩 시 `actor=admin` 관계를 명시적으로 전달함으로써, SQLAlchemy `before_insert` 리스너가 세션 플러시 시점에 수행자 정보를 정상 탐색하여 스냅샷으로 영구 기록하도록 보장했습니다.
  - **5) 시스템 설정 및 비밀키 일관성(`key_hash_snapshot`) 자동 설정**: 시딩 단계에서 `SystemSettings` 디폴트 데이터를 누락 없이 생성하고, 현재 적용된 비밀키 해시(`get_encryption_key()` 해시 또는 평문일 때 `"PLAINTEXT_MODE"`)를 `key_hash_snapshot`에 즉시 기록하여 시딩 후 웹 서버 기동 시 비밀키 일치 여부 불일치로 인한 Fail-Fast 중단 오류를 예방했습니다.
- **통합 검증 및 회귀 테스트**:
  - `seed_test_data.py --reset`을 통해 정상 시딩이 동작함을 검증하고, `run_remaining_tests.py` 통합 회귀 테스트 17종 전원 패스(**PASS**)를 확인하여 무결성을 보장했습니다.
- **레거시 `is_admin` 필드 및 권한 폴백 참조 완전 제거**:
  - 정식 출시 전 단계이므로 데이터 구조의 극단적 단순함과 명확성을 수호하기 위해, 기존 호환용 폴백 필드로 존치되던 `Users.is_admin` 컬럼을 테이블 스키마에서 완전히 들어냈습니다.
  - 이로 인해 영향을 받을 수 있는 모든 권한 검증 및 세션 체크 로직([dependencies.py](file:///w:/Documents/Project/SHIM/src/app/dependencies.py), [api_user.py](file:///w:/Documents/Project/SHIM/src/app/routers/api_user.py), [main.py](file:///w:/Documents/Project/SHIM/src/app/main.py)) 상의 `is_admin` 참조를 전수 색출하여 제거하고, 순수하게 `role == "ADMIN"` 단일 컬럼 조건으로 일원화했습니다.
  - 테스트용 데이터 시딩 스크립트([seed_test_data.py](file:///w:/Documents/Project/SHIM/tools/scripts/seed_test_data.py)) 내 admin 유저 생성 부의 `is_admin=True` 지정을 제거했습니다.
  - 단정 검사 스크립트([verify_refactoring.py](file:///w:/Documents/Project/SHIM/tools/scripts/verify_refactoring.py)) 내의 레거시 `is_admin` 검증 단계를 완전히 걷어내고 `role == "ADMIN"` 단일 검증으로 보정했습니다.
  - `seed_test_data.py --reset` 및 `verify_refactoring.py` 자가 정합성 단정 검사를 모두 무사 통과시켰으며, `run_remaining_tests.py` 통합 회귀 시나리오 17종 전원 패스(**PASS**)를 다시 한번 확인하여 권한 일원화에 따른 사이드 이펙트가 0%임을 최종 확인했습니다.

### v1.6.3 글로벌/오픈소스 대비 최종 아키텍처 개편 및 핫 백업 안정화
- **글로벌 타임존(Timezone) Naive UTC 단일화**:
  - `models.py` 내의 시간 필드 기본값 및 업데이트 시간 필드(`default`, `onupdate`)를 `lambda: datetime.datetime.now(datetime.timezone.utc).replace(tzinfo=None)`로 변경하여 SQLite 상에 타임존 정보가 없는 UTC DateTime 형태로 저장되도록 수정했습니다.
  - 연차 신청 대상 날짜(`Leaves.date`)는 타임존 오프셋 연산을 배제한 순수 날짜(Naive Date) 형태로 완벽하게 격리 보존하여 시차 오류를 방지했습니다.
- **인메모리 무상태(Stateless) 사용자 검색 엔진 구현 및 관리자 화면 연동**:
  - `utils.py`에 쌍자음(`ㄲ, ㄷ, ㅃ, ㅆ, ㅉ`)이 포함된 초성 검색 및 부분 문자열 일치를 지원하는 `search_users_stateless` 검색기를 구현했습니다.
  - `api_admin.py`의 `admin_users` 라우터 함수에 검색어 인자 `q: str = ""` 추가 및 `utils.search_users_stateless` 검색 엔진을 연동했습니다.
  - `admin_users`의 정렬 링크 및 템플릿 context에 `q` 검색어 유지 로직을 적용하여 검색 상태가 깨지지 않도록 하였습니다.
  - `src/templates/admin_users.html`에 Dense UI 테마에 맞춘 이름/초성 검색 입력창을 통합 배치했습니다.
- **사원 비활성화 시 미래 연차 일괄 취소 (Soft Delete 연쇄 제어)**:
  - `api_admin.py`의 `toggle_user_active` API에서 사용자 비활성화(`is_active=False`로 전환) 시, 해당 사용자의 오늘 및 미래 연차 일정(`date >= date_cls.today()`) 중 활성/대기 건들을 `status = "CANCELED"`로 업데이트하는 로직을 구현했습니다.
  - `api_admin.py`의 `update_user` API에서 사용자 비활성화 전환 시 오늘 및 미래 연차를 일괄 `CANCELED` 처리하는 연쇄 제어 장치를 동일하게 적용했습니다.
- **데드락 방지 및 WAL 체크포인트 병합이 보장되는 안전한 핫 백업**:
  - `ops.py`의 `create_sqlite_backup` 함수를 수정하여 `sqlite3.connect` 시 `timeout=30.0` 설정을 추가해 락 충돌 대기 시간을 확보했습니다.
  - 백업 개시 전 `src_conn.execute("PRAGMA wal_checkpoint(PASSIVE);")`를 명시적으로 가동하여 임시 WAL 저널의 데이터 페이지를 본체 데이터베이스 파일에 완전 병합한 뒤 복제하도록 구조화했습니다.
  - Python 표준 라이브러리 규격인 `src_conn.backup(dest_conn, pages=50, sleep=0.02)` 형식을 채택하여, 타 트랜잭션 경합을 방지하고 점진적으로 안전하게 백업되도록 API 호출 오류를 완전히 수정했습니다.
- **Windows Excluded Port Range 소켓 에러 해결 (WinError 10013)**:
  - Windows OS의 Hyper-V 포트 예약 범위(`7912 ~ 8011`)에 기존 기본 포트인 `8000`번이 포함되어 있어 개발 서버 구동 시 `WinError 10013` 바인딩 거부 예외가 발생하는 환경적 충돌을 진단했습니다.
  - [tools/scripts/dev.ps1](file:///w:/Documents/Project/SHIM/tools/scripts/dev.ps1) 내 uvicorn 바인딩 포트를 예약 제외 구역인 `8080`으로 수정하여 소켓 권한 충돌 문제를 완벽히 조치했습니다.
- **감사 로그(Audit Logs) 하이브리드 타임존 렌더링 누락 조치**:
  - 다른 화면들과 달리 감사 로그 화면([admin_audit.html](file:///w:/Documents/Project/SHIM/src/templates/admin_audit.html))에서만 UTC 시각을 `strftime`으로 직접 하드코딩 렌더링하고 있던 누락 사안을 발견했습니다.
  - 이를 수정하여 `data-utc` 및 `format_datetime_kst` 필터를 결합한 하이브리드 방식으로 일관되게 마이그레이션하여, 타국 사용자가 접속할 때도 브라우저 타임존으로 자동 동적 보정되도록 개선했습니다.