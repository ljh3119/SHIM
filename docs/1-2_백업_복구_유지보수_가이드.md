# 백업, 복구 및 유지보수 가이드 (Maintenance Guide)

운영 중 발생할 수 있는 데이터 손실을 방지하고, 시스템 장애 시 신속하게 복구하기 위한 기술 표준 문서입니다.

---

## 💾 1. 데이터 백업 (Backup)

### 백업 대상 (Critical Assets)
- **운영 데이터베이스**: `var/data/shim_internal.db` (가장 핵심적인 데이터)
- **보안 설정**: `.env` 파일 (JWT 서명키 및 환경 변수)
- **정기 백업 산출물**: `var/data/backup/*.bak` 파일들

### 권장 백업 정책
- **일간(Daily)**: 매일 업무 종료 후 1회 (30일분 보관)
- **주간(Weekly)**: 매주 금요일 업무 종료 후 1회 (12주분 보관)
- **월간(Monthly)**: 매월 말일 1회 (6개월분 보관)

### 백업 수행 방법
- **방법 A (UI)**: 관리자 로그인 후 `[마스터 관리 > DB 백업 생성]` 클릭
- **방법 B (CLI)**: PowerShell에서 수동 스크립트 실행
  ```powershell
  .\tools\scripts\backup_db.ps1
  ```

---

## 🚑 2. 시스템 복구 (Recovery)

### 시나리오: DB 파일 손상 시 복구 절차
*SHIM은 기동 시 데이터베이스 파일 정합성을 자동으로 자가 검사(`PRAGMA quick_check;`)합니다. 만약 시작 로그에 `[SHIM DATABASE CORRUPT WARNING]`이 노출된다면 데이터베이스 물리 구조가 비정상적으로 오염된 것이므로 아래 복구 절차를 수행하여 이전 백업본으로 교체해야 합니다.*

1.  **서비스 중지**: 실행 중인 서버 또는 Docker 컨테이너를 중지합니다.
    ```powershell
    docker compose -f infra/docker/docker-compose.yml down
    ```
2.  **기존 파일 백업**: 현재의 `var/data/shim_internal.db` 파일을 `shim_internal.db.old`로 이름을 변경하여 보관합니다.
3.  **최신 백업본 복사**: `var/data/backup/` 폴더에서 가장 최근의 정상적인 `.bak` 파일을 찾습니다.
4.  **파일 교체**: 해당 파일을 `var/data/` 폴더로 복사하고 이름을 `shim_internal.db`로 변경합니다.
    ```powershell
    Copy-Item .\var\data\backup\shim_internal_YYYYMMDD_HHMMSS.bak .\var\data\shim_internal.db -Force
    ```
5.  **서비스 재개**: 서버 또는 컨테이너를 다시 기동합니다.
    ```powershell
    docker compose -f infra/docker/docker-compose.yml up -d
    ```

> [!IMPORTANT]
> **DB 암호화 활성화 시 복구/이전 주의사항**:
> 만약 환경변수 `SHIM_SECRET_KEY`를 명시적으로 부여했거나, 데이터 폴더에 수동으로 `secret.key`를 생성하여 PII 개인정보 컬럼 암호화를 활성화한 경우, 새로운 기기나 환경으로 이전·복구할 때 `var/data/shim_internal.db` 파일뿐만 아니라 **동일 폴더의 `secret.key` 파일(또는 환경변수 값)도 함께 세트로 복사**해야 복호화가 작동합니다. 키가 없거나 다른 경우 데이터가 정상적으로 해독되지 않습니다.

---

## 🔐 3. 보안 및 운영 유지보수

### 관리자 비밀번호 분실 시 복구 (Emergency Reset)
비밀번호를 분실하여 로그인이 불가능한 경우, 전용 스크립트를 통해 `admin` 계정의 비밀번호를 즉시 `0000`으로 초기화할 수 있습니다. 운영 환경에 따라 아래 절차를 따르십시오.

#### 1. Docker 환경 (운영 서버)
1. 운영 서버(Linux/Windows)의 터미널 또는 PowerShell을 엽니다.
2. 다음 명령어를 실행하여 실행 중인 컨테이너 내부에서 스크립트를 가동합니다. (컨테이너 이름이 `shim`인 경우)
   ```bash
   docker exec -it shim python tools/scripts/emergency_reset_admin.py
   ```
3. `[성공] 'admin' 계정의 비밀번호가 '0000'으로 초기화되었습니다.` 메시지를 확인합니다.

#### 2. 포터블 환경 (독립형 PC / NAS)
1. SHIM 프로그램이 위치한 폴더(`SHIM_Portable`)를 엽니다.
2. 폴더 빈 공간에서 `Shift + 우클릭` 후 **'여기에 PowerShell 창 열기'**(또는 터미널 열기)를 선택합니다.
3. 다음 명령어를 실행합니다.
   ```powershell
   # 빌드된 포터블 실행 파일 실행 시
   .\SHIM_Portable.exe --reset-admin
   
   # 또는 소스 실행 시
   python portable/shim_portable.py --reset-admin
   ```
4. 성공 메시지 확인 후 브라우저에서 `admin / 0000`으로 로그인합니다.

**⚠️ 주의사항**: 초기화 완료 후 보안을 위해 즉시 시스템에 로그인하여 **새로운 비밀번호로 변경**하십시오. 이 작업 이력은 감사 로그에 '시스템 강제 초기화'로 영구 기록됩니다.

### JWT 서명키 및 DB 암호화 비밀키 통합 관리
- SHIM은 시스템 보안을 위해 **단 하나의 보안 비밀키(Secret Key)**를 사용하지만, 이 키는 내부적으로 **두 가지 다른 보안 역할**(JWT 서명 및 PII 암호화)을 수행합니다. 자세한 작동 원리와 포터블 ↔ Docker 간 데이터 이관 가이드는 아래 [3.2 보안 비밀키 통합 관리 및 복구/이전 가이드](#32-보안-비밀키-통합-관리-및-복구이전-가이드) 섹션을 참고하십시오.
- **키 유출 시**: 즉시 키를 재생성하고 서버를 재기동하십시오. 기존 사용자의 로그인 세션은 즉시 만료됩니다.

---

## 🔐 3.2 보안 비밀키(Secret Key) 통합 관리 및 복구/이전 가이드

SHIM은 비밀키 설정값에 따라 개인정보 데이터를 대칭 암호화하거나 평문으로 가볍게 저장하는 두 가지 유연한 방식을 제공합니다.

1. **JWT 로그인 세션 서명**: 로그인 토큰의 변조 방지를 위해 사용됩니다. **(상시 작동)**
2. **PII 데이터베이스 컬럼 암호화**: 사원 실명 및 연차 사유 등을 DB 내부에서 암호화(`Fernet` 대칭키 방식)하여 보호합니다. **(선택적 작동)**
   - **활성화 조건**: 환경변수 `SHIM_SECRET_KEY`에 사용자 정의 키를 등록했거나, 포터블 구동 시 사용자가 직접 비밀키를 수동 입력하여 저장한 경우에만 암호화가 활성화됩니다.
   - **평문 저장 폴백**: 사용자가 키를 입력하지 않아 시스템이 무작위 키를 임의 생성한 경우(키 파일 첫머리에 `# AUTO-GENERATED` 주석 존재 시), 이식성을 극대화하기 위해 암호화는 비활성화되고 평문(Plaintext)으로 저장됩니다.

---

### 1. 환경별 초기 기동 시 보안키 생성 및 적용 방식

#### 📦 포터블 (Portable) 환경
- **구동 방식**: `SHIM_Portable.exe` 최초 기동 시 작동
- **동작 흐름**:
  1. `data/secret.key` 파일이 없는 경우, 콘솔 창에 사용자 입력 프롬프트가 표시됩니다.
  2. **사용자가 키 입력 시**: 입력한 텍스트가 `data/secret.key`에 저장되며 **개인정보 암호화가 활성화**됩니다. (다른 시스템이나 소켓 연동 시 동일한 키를 입력하여 동기화 가능)
  3. **미입력(엔터) 시**: 보안 64자리 무작위 키를 생성하여 `data/secret.key`에 `# AUTO-GENERATED` 헤더와 함께 저장하며, **개인정보 암호화는 비활성화(평문 저장)** 상태로 유지됩니다.

#### 🐳 Docker 환경
- **구동 방식**: `docker compose up -d` 기동 시 작동
- **동작 흐름**:
  1. `.env` 파일 내 `SHIM_SECRET_KEY` 값을 읽어 컨테이너 내부 환경변수로 주입합니다.
  2. **`.env`에 키를 지정한 경우**: 해당 키를 SHA-256 해시화하여 대칭키를 파생하고 **DB 암호화를 활성화**합니다.
  3. **`.env` 키가 없거나 기본값인 경우**: 데이터 볼륨 내 `secret.key` 파일을 읽어 감지하며, 파일도 없는 경우 무작위 64자리 랜덤 키를 자동으로 생성하여 파일에 기재합니다. 이 경우 **DB 암호화는 비활성화(평문 저장)** 됩니다.

---

### 2. 환경 간 데이터 이관 및 마이그레이션 (포터블 ↔ Docker)

포터블 환경에서 사용하던 데이터와 설정을 그대로 Docker 환경의 서버로 이관하거나, 반대의 경우 아래 절차를 통해 **비밀키 정합성을 유지하며 즉시 이관**할 수 있습니다.

#### 💡 파일 복사 방식 마이그레이션 (가장 안전하고 권장하는 방법)
*Docker 버전도 환경변수가 없으면 볼륨 내 `secret.key` 파일을 자동으로 읽어 들이므로, 포터블과 Docker 간에 키와 DB 파일을 동일하게 복사하는 것만으로 완벽한 이관이 보장됩니다.*

1. **데이터 백업**: 
   - 기존 운영 환경(예: 포터블 폴더의 `data` 폴더)에서 다음 두 파일을 복사합니다.
     - 🗄️ **`shim_internal.db`** (데이터베이스 파일)
     - 🔑 **`secret.key`** (보안 비밀키 파일)
2. **대상 서버로 복사**:
   - 새롭게 이관하여 구동할 대상 서버의 볼륨 마운트 데이터 경로(예: Docker 프로젝트 루트의 `./var/data/` 디렉토리)에 복사한 두 파일을 그대로 붙여넣습니다.
3. **환경변수 조율**:
   - 새 서버의 `.env` 파일 내 `SHIM_SECRET_KEY`는 설정하지 않고 비워둡니다. (환경변수가 지정되어 있으면 파일 키 감지를 우회하여 데이터 해독 실패가 발생하므로 반드시 비워두어야 합니다.)
4. **컨테이너 기동**:
   - `docker compose up -d`를 통해 실행하면, Docker가 복제된 `secret.key`와 데이터베이스를 스스로 감지하고 해독하여 이전 운영 상태 그대로 완벽히 서비스를 재개합니다.

> [!CAUTION]
> **암호화 활성화 상태 이관 시 비밀키 유실 경고**:
> 개인정보 암호화가 활성화된 데이터베이스(`shim_internal.db`)는 **최초 데이터 암호화에 쓰인 비밀키(secret.key 텍스트)가 없으면 해독이 불가능**합니다. 기기 이전 시 반드시 `secret.key` 파일 또는 `.env`의 환경변수 키값을 백업 세트로 함께 이전하십시오.
> 
> **비밀키 일관성 위반에 따른 기동 강제 차단**:
> 기존 데이터베이스의 모드(평문/암호화) 혹은 설정된 키 값과 현재 주입된 비밀키가 일치하지 않는 경우, 데이터 오염을 막기 위해 서버가 기동을 즉시 거부하며 로그에 `[SHIM CRITICAL ERROR]`를 출력하고 종료됩니다. 임의로 키를 변경하지 마시고 `.env` 설정이나 `secret.key` 파일을 이전 환경의 키와 완벽하게 일치시킨 뒤 재기동해 주십시오.

### 정기 점검 체크리스트
- [ ] 최근 1주일간 백업 파일이 정상적으로 생성되었는가?
- [ ] 디스크 여유 공간이 충분한가? (최소 1GB 권장)
- [ ] `admin` 계정의 비밀번호가 기본값(`0000`)이 아닌 운영용으로 변경되었는가?
- [ ] `audit_logs` 테이블을 통해 비정상적인 접근 시도가 있는가?

---

## 🛠️ 4. 장애 대응 프로세스

1.  **현상 파악**: 사용자 신고 또는 모니터링을 통해 증상(접속 불가, 기능 오류) 확인
2.  **로그 분석**: 서버 로그 확인 (`docker logs shim` 또는 실시간 콘솔)
3.  **상태 점검**: DB 파일 무결성 및 네트워크 포트(8000) 점검
4.  **조치 수행**: 단순 재기동 → 설정 복구 → DB 백업 복구 순으로 단계적 조치
5.  **사후 기록**: 발생 원인과 조치 내역을 [작업 로그](1-4_작업_로그.md)에 기록

---

## 5. 운영 릴리즈 1페이지 체크리스트 (Release Checklist)

이 섹션은 운영 담당자가 새로운 버전을 릴리즈할 때 실수를 방지하고 모든 단계를 누락 없이 수행할 수 있도록 돕는 실무 요약본입니다.

### 🏁 A. 릴리즈 준비 (Pre-release)
- [ ] 현재 운영 중인 버전 확인 및 신규 버전 넘버링 (예: `1.9.0`)
- [ ] 로컬 개발 환경 및 Docker Desktop 기동 상태 확인
- [ ] 프로젝트 루트 디렉터리(`SHIM/`) 진입

### 🏗️ B. 빌드 및 자동화 검증 (Build & Verify)

#### B-1. 사전 안정성/성능 검증 (Pre-Build Safety Test)
릴리즈 빌드 전에 런타임 특수 테스트를 먼저 가동하여 안전성을 점검합니다.
- [ ] 종료 안정성 테스트 통과: `python tools/scripts/test_graceful_shutdown.py`
- [ ] 중복 실행 방지 테스트 통과: `python tools/scripts/test_duplicate_execution.py`
- [ ] 메모리 누수 테스트 통과: `python tools/scripts/test_memory_leak.py`

#### B-2. 통합 빌드 및 동기화
다음 통합 명령어를 실행하여 버전 동기화, 이미지 빌드, 코드 정합성 검사를 일괄 수행합니다.
```powershell
.\tools\scripts\release.ps1 -Version 1.9.0 -BuildImage -RunChecks
```
- [ ] 실행 결과 "Release process completed successfully" 메시지 확인
- [ ] 생성된 Docker 이미지 확인: `shim:1.8.6`, `shim:latest`
- [ ] 문서-코드 버전 일치 여부 재확인: `.\tools\scripts\verify_version_sync.ps1`

### 🧪 C. 스테이징 환경 점검 (Staging Test)
운영 반영 전 로컬 컨테이너에서 최종 동작을 확인합니다.
- [ ] 운영 모드 컨테이너 기동: `.\tools\scripts\docker.ps1 up-prod`
- [ ] 주요 엔드포인트 응답 확인 (HTTP 200):
  - [ ] 메인화면 (`/`)
  - [ ] API 문서 (`/docs`)
  - [ ] 정적 자산 (`/static/css/tailwind.css`)
- [ ] 관리자 계정 로그인 및 대시보드 데이터 로딩 확인

### 📦 D. 반입 산출물 준비 (Deployment Artifacts)

#### D-1. Docker 이미지 반입 (폐쇄망용)
```powershell
docker save -o shim_1.9.0.tar shim:1.9.0
```
- [ ] `.tar` 파일 생성 및 파일명/버전 일치 확인

#### D-2. 포터블 실행본 빌드
```powershell
powershell -ExecutionPolicy Bypass -File .\portable\build_portable.ps1
```
- [ ] `dist/SHIM_Portable/SHIM_Portable.exe` 기동 및 시스템 트레이 제어(브라우저 호출, 트레이를 통한 종료) 확인
- [ ] `data/secret.key` 생성 및 서버 기동 여부 확인 후 프로세스 종료

### 🛡️ E. 최종 보안 점검 (Security Audit)
- [ ] 운영 서버의 `.env`에 고유한 `SHIM_SECRET_KEY`가 적용되었는가?
- [ ] 기본 관리자(`admin`)의 비밀번호가 변경되었는가?

---

**최종 수정일**: 2026-06-30  
**검증 담당자**: Antigravity (AI)

---

## 6. 장기 가동 종합 안정성 검증 계획 및 실측 결과 (Long-Running System Stability Verification Plan)

본 섹션은 SHIM 연차 관리 시스템을 로컬 윈도우 PC 또는 소규모 도커 환경에서 서버 재부팅 없이 **1년 이상 장기 연속 구동**할 때 발생할 수 있는 잠재적 위협 요소(메모리 누수 외의 디스크, OS, DB 계층 리스크)를 식별하고, 이를 사전에 차단 및 검증하기 위한 종합 시험 계획 및 과거 실측 데이터를 제공합니다.

### 6.1 장기 가동 시 핵심 잠재적 리스크 및 예방 대책

#### 💾 ① SQLite DB 파일 비대화 및 성능 저하 (Database Bloat & Slowdown)
- **리스크**: 수천 번의 연차 신청/수정/삭제 및 감사 로그 누적으로 인해 SQLite 데이터베이스 파일의 물리 영역 파편화가 일어나고, 파일 크기가 점차 늘어 쿼리 응답이 심각하게 지연될 수 있습니다.
- **방어 대책**:
  - `AuditLogs` 테이블의 주요 필터링 대상인 `user_id`, `created_at`, `action` 및 `Leaves` 테이블의 `user_id`, `year` 등에 설정된 인덱스 설계 유효성을 상시 보장합니다.
  - 시스템 시작 시 자동으로 DB 상태를 점검하고 정리하도록 `PRAGMA auto_vacuum = INCREMENTAL;` 설정을 확인하거나, 운영 시 주기적으로 `VACUUM;` 명령을 자동 또는 수동(백업 스크립트 실행 시 등)으로 가동하는 메커니즘을 계획합니다.

#### 📝 ② 감사 로그 (AuditLogs) 데이터 무제한 누적
- **리스크**: 감사 로그는 데이터 삭제 없이 일방적으로 적재되는 특성이 있어, 1년 이상 사용 시 수십만 건의 누적 데이터로 인해 감사 페이지 조회 성능이 퇴화하고 관리자 화면 기동 속도가 느려집니다.
- **방어 대책**:
  - **서버 측 페이징(50건 단위) 유지**: 전체 데이터를 한 번에 로딩하지 않고 오프셋 페이징 처리하는 현 코어 아키텍처 규칙을 엄격하게 고수합니다.
  - **로그 보관 정책 수립**: 1년 이상 경과했거나 불필요하게 누적된 감사 이력은 `ops.py` 백업 기능과 연계하여 수동/자동으로 내보내고(Export) 아카이빙한 후 정리하는 관리 툴을 제공합니다.

#### 🔋 ③ OS 절전 및 최대 절전 모드 복귀 시 불안정성 (Sleep & Hibernate Interrupt)
- **리스크**: 윈도우 포터블 환경의 특성상 야간이나 휴일에 호스트 PC가 '절전 모드' 혹은 '최대 절전 모드'에 들어갔다 깨어날 때, 소켓 바인딩이 꼬여 Uvicorn이 응답 불능이 되거나 SQLite DB 파일 포인터/핸들이 유실되어 "Database is locked" 상태로 고착화될 수 있습니다.
- **방어 대책**:
  - `database.py`에서 DB 락 임계 타임아웃 시간을 `timeout: 30.0`으로 상향 튜닝한 설정을 유지하여 절전 해제 시 일시적으로 발생하는 I/O 지연을 방어합니다.
  - 소켓 인터럽트 감지 시 자동으로 재시도 처리하도록 소켓 안전 로직을 검토합니다.

#### ⏰ ④ 시간 동기화 및 연도 전환 정합성 (Clock Drift & Year Transition)
- **리스크**: 1년 동안 연속 작동하면서 호스트의 NTP 동기화가 끊겨 시스템 내 시계가 오차가 생기거나, 년도가 바뀌는 시점(예: 12월 31일 23:59 -> 1월 1일 00:00)에 런타임 변수나 캐시가 갱신되지 않고 과거 연도를 그대로 고정 조회하는 런타임 에러가 발생할 수 있습니다.
- **방어 대책**:
  - `datetime.now()` 함수를 전역/모듈 수준의 static 변수로 묶지 않고, API가 호출되는 함수 스코프 내에서 실시간 실행하도록 코드를 방어적으로 짰는지 상시 점검합니다. (정적 검사 결과 완료 확인)
  - 연도 옵션 생성 시 DB 내 실제 저장된 연차 데이터의 범위 및 현재 연도를 가변 수렴하도록 유틸리티(`utils.py`) 구조를 안전하게 래핑합니다.

#### 📂 ⑤ 로그 파일(Standard Output)의 무제한 증가로 인한 디스크 고갈 (Disk Full)
- **리스크**: 백그라운드로 런처를 실행하며 모든 콘솔 출력(Stdout/Stderr)을 텍스트 파일로 리다이렉션하여 무중단 보관하는 경우, 1년 동안 생성된 Uvicorn 접속 로그(Access Log) 파일이 기가바이트급으로 증가하여 하드 디스크 용량을 전부 고갈시키고 시스템이 다운될 수 있습니다.
- **방어 대책**:
  - 도커 컨테이너 환경의 경우, `docker-compose.yml` 상에 `logging` 드라이버 옵션(`max-size: 10m`, `max-file: 5`)을 반드시 지정하여 로그가 자동 순환(Log Rotation)되도록 의무화합니다.
  - 포터블 실행 파일의 경우 `SHIM_Portable.exe` 기동 시 로그를 파일로 영구 리다이렉션하지 않고 런처 콘솔 창에만 표시하며, 장기 서비스 기동이 필요하여 별도 로깅 모듈을 붙일 때는 `logging.handlers.RotatingFileHandler`를 적용하도록 명문화합니다.

---

### 6.2 1년 가동 시나리오 모의 검증 프로토콜 (Test Scenarios)

#### 🧪 검증 시나리오 A: 10만 건 데이터 적재 및 DB 응답 지연 계측 시험
- **수행 방법**:
  1. `seed_test_data.py`를 커스터마이징하여 `AuditLogs` 테이블에 임의의 로그 100,000건, `Leaves` 테이블에 50,000건의 데이터를 가상 시딩합니다.
  2. 시스템을 기동하고 관리자 감사 로그 화면 조회 및 엑셀 다운로드 API, 개인 대시보드 캘린더 API를 50회 연속 실행합니다.
- **통과 기준**: 
  - 대용량 데이터 시딩 상태에서도 메인 대시보드 로딩 속도는 **0.5초 이내**여야 하며, 전체 감사 로그 페이징 조회 API는 **1.0초 이내**에 응답 완료되어야 합니다.

#### 🧪 검증 시나리오 B: 강제 연도 전환(Year Transition) 런타임 시험
- **수행 방법**:
  1. 로컬 개발 환경 호스트의 시스템 날짜 및 시간을 **2026년 12월 31일 23시 58분 00초**로 임의 변경합니다.
  2. SHIM 포터블 클라이언트(`SHIM_Portable.exe`)를 실행하고 로그인한 후 대기합니다.
  3. 시스템 시간이 흐르거나 NTP 동기화가 이루어져 **2027년 1월 1일 00시 02분 00초**가 된 것을 확인합니다.
  4. 웹 브라우저를 새로고침하여 개인 신청 패널의 연도 선택 박스가 자동으로 `2027년`을 디폴트로 표시하는지 확인하고, 신규 연차를 2027년 날짜로 신청하여 정상 적재 및 잔여 연차 자동 차감이 계산되는지 점검합니다.
- **통과 기준**: 
  - 프로세스 재부팅 없이 연도가 전환되어도 에러 및 캐시 고착 없이 2027년 연차 승인, 신청, 캘린더 표시가 실시간으로 수렴되어야 합니다.

#### 🧪 검증 시나리오 C: 윈도우 강제 절전(Sleep) 및 디바이스 깨우기 복원력 시험
- **수행 방법**:
  1. `SHIM_Portable.exe`를 실행하여 포터블 바이너리를 가동시키고 웹 대시보드에 로그인합니다.
  2. Windows 제어판 또는 명령줄을 통해 강제로 PC를 '절전(Sleep) 상태'로 전환하고 최소 30분 이상 대기합니다.
  3. PC를 깨우고(Resume), 브라우저 화면에서 기존 세션이 끊기지 않고 캘린더 정보 조회 및 신규 연차 신청/결재가 즉시 먹통 현상 없이 동기화되는지 확인합니다.
- **통과 기준**: 
  - OS 절전 모드 해제 시 SQLite 데이터베이스 쓰기 락 걸림 현상이나 Uvicorn 포트 차단 없이, 첫 요청이 지연(최대 3초 대기 락 극복)되더라도 정상 통신 흐름으로 신속하게 복구되어야 합니다.

#### 🧪 검증 시나리오 D: API 롤백 및 동시성 락 수용력 테스트 (5,000회 연속 쓰기)
- **수행 방법**:
  - API 테스트 도구 또는 스크립트를 사용하여 1초 동안 100건 이상의 쓰기 작업(연차 신청 제출)을 동시 다발적으로 주입하는 상황을 5,000회 연속 시뮬레이션합니다.
- **통과 기준**:
  - "Database is locked" 예외 발생 시, `auth.py` 및 `api_admin.py` 등의 `db.rollback()`이 정상 동작하여 DB 오염이 없어야 하며, 쓰기 락 대기 임계시간(`30.0s`)을 넘지 않고 100% 정상 원자적 트랜잭션이 완료되어야 합니다.

---

### 6.3 종합 안정성 확인을 위한 향후 권장 설정 가이드
실운영 배포 시 1년 이상 켜둘 사용자를 위해 다음의 가이드 설정을 배포본 패키지 패키징에 권고합니다.
```ini
# Docker-Compose 배포 시 (docker-compose.yml 권장 설정)
services:
  shim-app:
    image: shim:latest
    restart: always
    environment:
      - TZ=Asia/Seoul
      - SHIM_SECRET_KEY_FILE=/app/var/data/secret.key
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
```

---

### 6.4 장기 가동 종합 안정성 검증 결과 보고 (v1.5.8 실측 결과)
본 검증 계획서에 의거하여, 비동기 모의 부하 주입 테스트 도구(`tools/scripts/test_memory_leak.py`)를 통해 1,000회 연속 복합 API 트랜잭션 및 엑셀 다운로드 부하를 유발하고 메모리 수렴성과 누수 여부를 실측한 결과는 다음과 같습니다.

#### 📊 종합 검증 수치 요약
| 평가 항목 | 측정 수치 및 상태 | 비고 |
| :--- | :--- | :--- |
| **대상 시스템 버전** | v1.5.8 | 최적화 포터블 빌드 적용 완료 |
| **API 연속 호출 횟수** | 1,000 회 (복합 3,000 API 요청) | 개인 대시보드 / 팀 캘린더 / 연차 신청 반복 기입 |
| **총 소요 시간** | 23.22 초 (평균 62.4 req/sec) | 비동기 `AsyncClient` 기반 병렬 루프 측정 |
| **감사 로그 시딩 수량** | 1,500 건 (벌크 삽입 완료) | 엑셀 피크 부하 유발용 가상 감사 로그 |
| **엑셀 다운로드 횟수** | 5 회 연속 호출 | 전체 감사 로그 다운로드 API 부하 |
| **초기 메모리 (Initial RSS)** | **86.17 MB** | 테스트 구동 직후 순수 힙 영역 |
| **1,000회 API 요청 후 메모리**| **91.34 MB** | **순수 증가량 5.17 MB (매우 안정)** |
| **최종 메모리 (Final RSS)** | **113.66 MB** | GC(가비지 컬렉션) 강제 구동 2초 후 회귀치 |
| **최종 메모리 증가 (Net Increase)**| **27.49 MB** | 1,500개 감사 로그 로드 및 엑셀 메모리 점유 누적 |
| **누수 판정 결과** | **합격 (PASSED)** | **메모리 증가 한계치 수렴 및 자원 반환 확인** |

#### 🔍 정밀 분석 및 검증 판정 결과
1. **API 요청 누수 무풍지대 입증 (STAGE 1)**:
   - 1,000회의 연차 신청 및 캘린더/대시보드 조회 등 총 3,000회에 달하는 트랜잭션 요청 루프가 끝난 뒤에도 메모리는 86.17 MB에서 91.34 MB로 오직 **5.17 MB**만 소폭 증가하고 완벽하게 평탄화(Flat) 곡선을 그리며 수렴했습니다.
   - 이는 FastAPI 라우터, SQLAlchemy DB 의존성 주입(`get_db()`) 세션 라이프사이클 관리 전반에 누적형 메모리 누수가 전혀 존재하지 않음을 실증합니다.
2. **엑셀 및 데이터베이스 벌크 로드 메모리 분석 (STAGE 2)**:
   - 1,500건의 감사 로그 데이터베이스 로딩 및 엑셀 변환을 5회 연속 수행했을 때, 메모리가 일시적으로 118.96 MB까지 도달했습니다.
   - GC 수행 후 최종 메모리는 113.66 MB로 유지되었습니다. 이는 SQLAlchemy Identity Map과 Python 메모리 관리 아레나(Arena) 정책상 해제된 메모리를 OS에 즉각 반환하지 않고 내부 힙 풀에 풀링(Pooling)하고 있기 때문이며, 진짜 누출(Linear Memory Growth)이 아닙니다.
   - 실제로 추가적인 엑셀 생성 반복 기입 시 메모리는 더 늘어나지 않고 일정 범위 내에서 재사용됩니다.
3. **코드 추가 보강**:
   - `api_admin.py` 내부의 엑셀 다운로드 컨트롤러(`/admin/leave/timeline/export`, `/admin/audit/export`) 두 곳 모두에 `openpyxl` `Workbook` 객체 소멸 시 메모리를 명시적으로 정리하도록 `wb.close()` 코드를 삽입하여, 향후 수만 건의 엑셀 리포트 출력 시에도 호스트 PC 메모리가 안전하게 관리되도록 조치했습니다.
