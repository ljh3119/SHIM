# 운영/릴리즈 통합 산출물

본 문서는 완료된 릴리즈 증적, 검증 결과, 운영 인수 체크리스트만 관리합니다. 앞으로 할 일은 `docs/3-1_향후_개선계획.md`, AI 작업 규칙은 `docs/3-2_AI_인수인계_가이드.md`에서 관리합니다.

## 1) 현재 릴리즈 상태 (v1.3.2)

- **PM 자동 승인 및 사용자 UI 최적화(2026-05-13)**:
    - **PM 승인 로직 개선**: PM(프로젝트 매니저) 역할의 사용자가 연차 신청 시 시스템의 승인 필수 설정과 무관하게 즉시 자동 승인(`APPROVED`) 처리되도록 로직 고도화.
    - **사용자 대시보드 통계 강화**: `user_dashboard.html` 상단에 연도별 총 연차, 사용 시간, 잔여 시간, 신청 건수 요약 카드를 추가하여 사용자가 본인의 현황을 즉시 파악할 수 있도록 개선.
    - **팀 캘린더 UI 구조 개선**: 우리 팀 캘린더 화면의 연도/월 조회 필터를 별도의 상단 박스 패널로 분리하여 조작성을 높이고 캘린더 본연의 가시성 확보.
    - **로직 검증**: `verify_pm_logic.py`를 통해 역할별 자동 승인 분기 로직이 정상 작동함을 검증 완료.

- **이전 릴리즈 (v1.3.1)**:
    - Tailwind CSS v4 통합 및 의존성 업데이트(2026-05-13):
        - **엔진 업그레이드**: Tailwind CSS v4.x로 엔진 업그레이드 및 `@tailwindcss/cli` 도입을 통한 빌드 성능 향상.
        - **의존성 최신화**: `tailwindcss`, `@tailwindcss/cli` 버전을 `v4.3.0`으로 상향 조정.
        - **결재 권한 역할 확장**: 결재 가능 역할에 `PM`(프로젝트 매니저)을 추가하여 유연한 결재 체계 구축. 감사 로그(Audit Log) 액션명을 범용적으로 개선.
        - **전용 결재 페이지 및 UI 개편**: 대시보드에 밀집되어 있던 결재 관리 기능을 전용 페이지(`/user/approvals`)로 분리. 팀 캘린더 및 히스토리도 별도 메뉴로 분산하여 시인성 확보.
        - **신청 로직 결함 보정**: 연차 신청 시 반려(`REJECTED`)된 신청 건이 해당 날짜의 신청 가능 시간대 및 일일 합산 시간을 점유하던 문제를 수정하여 정상적인 재신청이 가능하도록 보정.
        - **빌드 정합성**: v4 엔진 기반 CSS 생성 및 포터블/Docker 환경 배포 정합성 검증 완료.

- **이전 릴리즈 (v1.3.0)**:
    - 역할 기반 권한 체계(RBAC) 및 팀 협업 강화(2026-05-12):
        - 4단계 역할 도입(`STAFF`, `TEAM_LEAD`, `PM`, `ADMIN`) 및 자동 마이그레이션 로직 구현.
        - 팀장 결재 워크플로우 및 팀 캘린더 공유 기능 추가.
        - 사용자 관리 UI 역할 배지 표시 및 관리 기능 확장.
        - 프로젝트 최종화 및 레거시 코드/데이터 정리 수행.

- **이전 릴리즈 (v1.2.1)**:
    - 리브랜딩 통합(2026-05-09): `edu_LAMS` → `SHIM` 전체 명칭 및 폴더 구조 전환 완료
    - 푸터 브랜딩: 하단 문구를 "Smart Holiday & Integrated Management"로 업데이트
    - 빌드 스크립트 수정: `build_portable.ps1` 내 복사 경로 버그 수정 및 최종 산출물 내 테스트 데이터 자동 배제 적용
    - 패키지/앱/템플릿/`docker-compose*` 기본 이미지 태그 `1.2.1` 동기화 유지
    - dense UI 가독성 보정: 텍스트 대비 토큰 상향(`--color-dense-muted`, `--color-dense-nav-text`) 및 경계선 대비 상향(`--color-dense-line`), 캘린더/공통 하드코딩 색상 동기화
    - 관리자 사이드바 토글 개선: `<`/`>` 문자 버튼을 chevron SVG 아이콘으로 교체, 버튼 위치/크기/배경 톤을 균형 조정
    - 승인 워크플로우 OFF 시 대시보드 정리: 우측 상단 `결재 대기 n건` 배지와 `결재 대기 건수` 카드 비노출, KPI 그리드 컬럼 수 자동 조정(4→3)
    - 시스템 브랜딩: `SystemSettings`에 공식명·내비 짧은 표기(`product_nav_short`)·브랜딩 배지 등, 마스터 화면 저장 API/폼, 부트 단계 마이레이션
    - 기존 SQLite 대응: `models.Base.metadata.create_all` 직후 `ensure_sqlite_system_schema()`로 누락 컬럼 보강, 불용 `product_user_sidebar_title` 제거 시도
    - 전역 브랜딩 노출을 위한 미들웨어·템플릿 컨텍스트 처리(`main.py`/Jinja)
    - 일반 사용자: `base.html`에서 사용자 사이드바 미포함 레이아웃 정리
    - 관리자·공통 shell: 관리자 사이드바 부제/접힘 정렬(`sidebar_admin.html` 등), 대시보드·사원·공휴일·마스터 등 dense UI 정렬(기존 URL·역할 유지)
    - 미배포 기준 레거시 정리: 미사용 `YearlyLeaveFinalizations` 모델, `src/app/schemas.py`, `partials/sidebar_user.html`, `brand_initial_mark` 제거
    - 사용자 스키마 정리: `users.department` 및 관련 fallback/백필 로직 제거, `company`/`team` 기준으로 화면·관리 API 정합
    - 타임라인(`admin_leaves_timeline`): 가로 스크롤 완화, 반려 사유 `line-clamp-2` 및 `title`로 전체 보기
    - 공휴일: 한국 공휴일 시드에 **5월 1일 노동절** 포함(연도별 KR 시드 최초 블록)
- 폐쇄망 기준 유지: 외부 CDN/웹폰트 미사용, 로컬 `tailwind.css`, `SHIM_SECRET_KEY` 지원
- Docker 검증(2026-05-03): `shim:1.2.0`(및 `latest`) 이미지 빌드, `infra/docker/docker-compose.yml`로 컨테이너 재생성 후 `docker compose ps`에서 `shim -> shim:1.2.0` 확인, `docker logs`로 startup 정상 확인, `docker exec`로 컨테이너 내부에서 `/docs` HTTP `200` 확인(일부 PC에서는 호스트 `localhost:8000` 바인딩이 지연·차단될 수 있어 내부 점검 병행)
- 포터블(2026-05-03): `portable/build_portable.ps1` 재빌드 완료, 산출 `artifacts/dist/SHIM_Portable` 갱신. 런타임 스모크는 `run_portable.bat`(포트 예: 8765) 기준으로 `/`, `/docs`, `/static/css/tailwind.css`, `/favicon.ico`, 로그인 후 `/admin/dashboard` 순서 점검 권장

선행 기능(v1.1.7 이하에서 유지되는 기준 요약):

- 시작/종료시간 입력 기반 신청, 시간 단위·업무시간·점심 제외 정책, 신청 전 예상 차감 확인, 결재 전이 서비스·반려 사유 정책, 운영 백업/성능 리허설·폐쇄망 UI 경로 등은 계속 적용된다. 세부 증적은 릴리즈별로 본 문서 과거 블록·`docs/4-1_SHIM_프로젝트_설계서.md` 참고.

## 2) 핵심 기능/구조 반영 요약

- 연차 이력은 `Leaves.snapshot_*` + `status` + `rejection_reason` 기반 저장
- 승인 워크플로우는 `SystemSettings.is_approval_required`로 제어
- 시간 정책은 `SystemSettings.time_granularity_minutes`, `work_start_minute`, `work_end_minute`, `lunch_start_minute`, `lunch_end_minute`로 제어
- 병합 정책(`LeavePolicies`)은 운영 단순화 방침으로 제거
- 관리자 화면에서 상태 변경, 승인 설정, 수동 DB 백업 수행 가능

## 3) 운영 정책: 신청 취소 vs 삭제

- `CANCELED`: 행 유지 + 상태만 변경(기록 보존)
- 삭제: 행 물리 삭제(복구 어려움)

권장 기준:

- 일반 운영은 취소 우선
- 삭제는 테스트/중복 오기입 같은 예외 상황에 한정
- 관리자 화면 라벨은 "영구 삭제(복구 불가)" 톤으로 유지

## 4) 릴리즈 체크리스트

### 데이터/스키마

- [x] `leaves`에 `snapshot_*`, `status` 컬럼 존재
- [x] `leaves.rejection_reason` 컬럼 존재(최대 500자 정책, 무인덱스)
- [x] `system_settings`에 `time_granularity_minutes`, `work_start_minute`, `work_end_minute`, `lunch_start_minute`, `lunch_end_minute` 컬럼 존재
- [x] `leaves` 핵심 조회 인덱스 4종 신규/기존 DB 적용 경로 반영
- [x] `system_settings` 테이블/초기값 유지
- [x] SQLite WAL 모드 활성화 유지
- [x] 슬롯/병합정책 테이블 제거 이후에도 재기동 정상 동작

### 기능

- [x] 사용자 신청 시 스냅샷 데이터 저장
- [x] 승인 토글 Off: 신규 신청 `APPROVED`
- [x] 승인 토글 On: 신규 신청 `PENDING`
- [x] 시작/종료시간 입력 신청 정상 동작
- [x] 단위 경계 외 입력(예: `15:15~16:15`) 차단
- [x] 최소 신청시간(정책 단위 미만) 차단
- [x] 점심시간 제외 정책 적용
- [x] 신청 전 확인 팝업(`신청하시겠습니까? (예상 차감: -Xh)`) 동작
- [x] 120분 정책에서 하루 연차(`09:00~18:00`) 허용 및 단축 구간(`17:00~18:00`) 차단
- [x] 관리자 상태 변경 동작 확인
- [x] `PENDING -> APPROVED/REJECTED/CANCELED`, `APPROVED -> CANCELED` 허용 전이 서비스화
- [x] `REJECTED -> APPROVED`, `CANCELED -> APPROVED` 기본 차단
- [x] 반려 처리 시 반려 사유 필수 입력, 공백-only/500자 초과 차단
- [x] 반려 사유 관리자 타임라인/사용자 대시보드 노출
- [x] 관리자 마스터에서 시간 정책(단위/업무시간/점심시간) 설정 가능

### 포터블

- [x] `SHIM_Portable.exe` 실행 시 서버 정상 기동
- [x] `/admin/dashboard` `200 OK` 확인
- [x] `runtime_base`가 포터블 `_internal` 경로로 표시되는지 확인
